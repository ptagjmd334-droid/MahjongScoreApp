# 開発ログ 2026-09-26

## M7 v43：牌種認識エンジン再設計
- v42で連続修正UIの「近い候補」同期がiPhone実機で解決したため、UI/14枚切り出しは固定し、牌種認識エンジンだけを次段階へ進めた。
- 直近実機ではv40系の高信頼自動候補は1/1正解だった一方、未確定13枚のTop3に正解が含まれたのは4/13。閾値調整だけではランキング品質が不足すると判断。
- v43では従来の「インク量グリッド中心」の216次元特徴を廃止し、正規化した牌面から以下を組み合わせる構造化特徴へ変更。
  - HOG：6×9セル×8方向。線の向き・形状を主成分（距離60%）。
  - 色：4×6セル×[赤優勢・緑優勢・暗さ]（距離25%）。
  - インク占有：8×12セル（距離15%）。
- HOG/色/インクそれぞれで±1セルの小さな位置ずれを許容し、最終距離を加重合成する。
- 自動確定は引き続きprecision優先。v43の新しい距離尺度に合わせて保守的な絶対距離・margin・ratio条件へ調整。
- 学習feature形式が変わるためlocalStorageの学習キーは `MahjongScoreApp_tile_templates_m7v43hog1` へ分離。v42以前のfeatureは混ぜない。
- 今後の再学習負担を減らすため、14枚を正しく確定した際に「正解ラベル＋正規化した牌画像」をIndexedDB `MahjongScoreAppM7Training` に保存する仕組みを追加。各牌種最大10画像。
- v43以降でfeature方式を再変更した場合、IndexedDBの保存画像から新featureライブラリを再構築できる基盤も実装。IndexedDBが利用できない環境では従来どおりlocalStorage featureだけで動作する。
- 14枚切り出し、撮影枠、修正UI、近い候補の連続更新は変更していない。

## 自動テスト
- pure unit：旧配列featureとの互換、HOG/色/インク構造化距離、robust multi-template ranking。
- Chromium横画面：牌面正規化、HOG 432要素・色72要素・インク96要素、照明差に対する同一形状の安定性、色情報の差、固定枠撮影→14枚編集、学習feature保存、IndexedDBへ14枚画像保存、連続修正Top3更新、通常対局/採点フロー。
- work branch GitHub Actions run 36155758939：全成功。

## 次のiPhone確認
1. 表示が **M7 v43** であることを確認。
2. v43はfeature形式が新しいため、初回だけ同じ14枚を撮影して14枚を正しく指定し確定する。
3. その後、14枚を並び替えてもう1回撮影。
4. 「高信頼候補数 / 正解数 / 誤り数 / 残りTop3に正解が入った数」を記録する。
5. 目標はまず高信頼候補precisionを維持しつつ、Top3 hit rateをv40の4/13から明確に改善すること。
