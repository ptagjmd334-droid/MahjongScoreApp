# MahjongScoreApp 既知ミス・再発防止ログ（正本）

このファイルを **MahjongScoreApp の既知ミス・再発防止の Source of Truth** として扱う。

## 運用ルール
- 新機能追加・バグ修正の前に、まずこのファイルを確認する。
- 変更内容が下記のどの項目に触れるかを確認し、該当する再発防止チェックを先に行う。
- バグが新しく見つかったら、**症状 / 根本原因 / 修正 / 再発防止 / 回帰テスト** をこのファイルへ追記してから、または修正と同じコミット群で更新する。
- 「直ったように見える」だけで完了扱いにしない。iPhone実機/PWAで確認する。
- 原因が未確定なものは「未確定」と明記し、推測を確定事項として残さない。
- 古いパッチを外す時は、そのパッチが担っていた別機能まで消えないかを確認する。
- 版数表示だけでなく、**index.htmlの読込対象 / 実ファイル / GitHub PagesのデプロイSHA** を確認する。
- このログが長くなっても、日常作業では別ファイル `docs/pre-change-checklist.md` の短いチェックを先に使い、必要な項目だけ本ログへ戻る。

---

# A. 開発手順・コード構造

## M001: 追記パッチ累積で同名関数・処理が競合し、全ボタン無反応
**時期:** 2026-09-12頃  
**症状:** 追加パッチ後、設定画面は出るがボタンが反応しない。  
**原因:** script.js末尾へ修正を積み重ね、同名関数の再定義・上書きやJS停止が発生しやすい構造になった。  
**修正/教訓:** 関連箇所だけを確認しつつ、変更後は全体構文チェック。数機能ごとに整理する。  
**再発防止:** 
- 同名関数を「とりあえず後ろで再定義」しない。
- 既存処理を差し替えるなら、どの旧処理を無効化するか明記。
- 新パッチを増やす時は、既存パッチとの責務重複を確認。
**回帰テスト:** 対局開始、アガリ、流局、リーチ、Undo、メニューの主要ボタンを一周。

## M002: 現在の実ファイルを確認せず推測で修正し、既存仕様を壊す
**症状:** 修正自体は正しそうでも、既存DOM/関数/読込順と合わず別の不具合が出る。  
**原因:** 直前の実ファイルではなく、過去の想定や会話上の状態を基準に変更した。  
**再発防止:** 変更前に必ず対象ファイルの関連箇所を取得する。必要なら index.html の読込順も確認。  
**回帰テスト:** 変更対象だけでなく、その前後1ステップも操作。

## M003: 古いパッチを外した時に、そのパッチが担っていた別機能まで消えた
**例:** v10を外した結果、body直下へのoverlay移動やプレイヤー選択補助まで失われ、v18のCSS前提が崩れた。  
**原因:** 1つのパッチが複数責務を持っていたのに、不要機能だけを消したつもりでファイル全体を外した。  
**再発防止:** 古いファイルを読込対象から外す前に「そのファイルが担う機能一覧」を確認し、必要機能を先に移植する。  
**回帰テスト:** 旧パッチが触っていたUI/操作をすべて再確認。

## M004: バージョン別パッチを積みすぎて競合し、原因特定が難しくなった
**症状:** v8/v18/v21等が同じ点数表や符表示を別々に装飾し、古い表示やクラスが残る。  
**原因:** バージョンごとの応急パッチを長期間同時読込した。  
**再発防止:** 
- 同一領域を3世代以上のパッチが触り始めたら整理候補。
- 新版で責務を一本化したら古い版は読込から外す。
- ただしM003の確認を先に行う。
**回帰テスト:** DOMに旧版ID/クラス/表示が残っていないか確認。

---

# B. バージョン・デプロイ・キャッシュ

## M005: 「ファイルは作った」だけで公開版に読み込まれていなかった
**例:** m8-v10.jsは存在したが、当時の index.html が v9表示のままで m8-v10.js を読んでいなかった。  
**原因:** ファイル作成と index.html 更新を別々に扱い、公開入口を確認していなかった。  
**再発防止:** 新バージョンでは必ず  
1. 新ファイル存在  
2. index.html読込  
3. バッジ  
4. cache-bust query  
5. Pages最新SHA  
をセットで確認する。

## M006: 古いスクリプトが新版バッジを後から上書き
**例:** 古いM8スクリプトが実行後にバッジを書き換え、実装と表示版数がズレた。  
**原因:** 複数パッチが `#app-build-badge` を書き換えていた。  
**再発防止:** バッジ更新は原則最新パッチ1箇所だけ。旧版のbadge書換えは無効化する。

## M007: v20の補助スクリプトが非同期で読み込まれ、v21表示をv20へ戻した
**症状:** v21を公開したのに左下がv20。  
**原因:** m8-v20.jsが後から m8-v20-stable.js を読み込み、バッジ/挙動を上書き。  
**再発防止:** 動的script挿入があるファイルを新バージョンで残さない。index.html以外からのバージョンスクリプト読込を検索する。

## M008: GitHub Pagesは成功しているのにiPhoneで旧版が見え続けた
**時期:** v11反映時。  
**症状:** Pages workflowはv11成功なのにSafari/PWAはv10。  
**確認事項:** 当時の sw.js は古い `mahjong-score-app-m7-v3` キャッシュ名で、`./` と `index.html` をキャッシュしていた。ただしこれ単独が根本原因だったとは確定していない。  
**再発防止:** 
- 更新問題では「GitHub上のindex」「Pages workflow」「Safari直URL」「PWA」の4層を分けて調べる。
- キャッシュ原因と断定する前にunique query/unique fileで切り分ける。
- sw.jsの古いキャッシュ名・shellキャッシュを放置しない。
**回帰テスト:** Safari直URLとホーム画面PWAの両方でバージョン一致。

## M009: Netlify無料枠/クレジットでデプロイが止まり、検証環境が混線
**症状:** Netlifyが新規デプロイできず、GitHub Pagesへ移行。  
**教訓:** 公開元が変わるとキャッシュ・URL・PWA挙動も変わる。  
**再発防止:** 実機テストの公開元はGitHub Pagesを正本として固定し、別ホストの結果を混ぜない。

## M010: デプロイ通知をアプリ更新問題の解決と混同
**症状:** 「更新されたら通知」の話と、実際のPWA更新反映問題が混ざった。  
**教訓:** 通知機能は反映速度を改善しない。  
**再発防止:** デプロイ完了通知、CDN反映、ブラウザ/PWAキャッシュ、アプリ内update検知を別問題として扱う。

---

# C. iPhone/PWAレイアウト・中央配置・スクロール

## M011: fixed要素がtransform祖先の影響を受け、アガリ画面が真ん中に来ない
**症状:** アガリ方法/和了者選択が上・左へズレる。  
**原因:** `#agari-overlay` が transformed ancestor 内にあり、fixedの基準がviewportにならなかった。  
**修正:** overlayをbody直下へ移す方式。  
**再発防止:** fixedモーダルはbody直下を基本にし、親transform有無を確認。

## M012: vw/dvhや物理画面サイズを基準に中央計算し、iPhone PWAでズレた
**症状:** 左半分、左下、右/下見切れ。  
**原因:** Safari/PWAの実表示領域と `vw/dvh` / 物理画面サイズが一致しないケース。  
**修正:** 実際に表示されている `#game-screen.getBoundingClientRect()` / visualViewportを基準にする。  
**再発防止:** 「中央」はCSS感覚ではなく、表示後にrectを実測して中心差を検証。

## M013: PWA復帰後にviewportが壊れ、下部空白・見切れ
**症状:** バックグラウンド復帰後、下に空白や右/下の見切れ。タスクキルで直ることがあった。  
**原因:** 復帰ライフサイクルでviewport再計算が不安定。  
**再発防止:** pageshow / visibilitychange / orientationchange後に実表示領域を再測定。ただし無制限な多重タイマーは避ける。

## M014: 点数表を小さくしすぎた
**例:** v17で `scale(.76)` を残し、中央には来たが表が小さい。  
**原因:** 「見切れ対策」を縮小だけで解決しようとした。  
**再発防止:** まず本文スクロール＋固定フッター。縮小は最後の手段で、実機で可読性確認。

## M015: 点数表の下ボタンをstickyにしたが、DOM上は途中にあり上へ浮いた
**症状:** 「戻る / 点数を直接入力 / 次へ」が表の途中に表示。  
**原因:** stickyは「DOM上の位置」を変えない。表の下に別要素がある状態で途中のactionsをsticky化した。  
**修正:** actionsをスクロール領域の外へ物理的に移し、実フッター化。  
**再発防止:** 固定したい操作はDOM構造も「header / scroll body / footer」にする。

## M016: 点数表フッターを別画面でも残し、ロン/ツモ画面にボタンが出た
**時期:** v13→v14。  
**症状:** 「戻る / 点数を直接入力 / 次へ」がロン/ツモ選択時にも表示。  
**原因:** score画面でreparentしたfooterを非score画面でcleanupしていなかった。  
**再発防止:** 画面モード切替時にscore専用DOM/classesを必ずcleanup。

## M017: CSSが `body > #agari-overlay` 前提なのにoverlayがgame-screen内に残り、スクロールが効かなかった
**症状:** 表下部が押せず、縦スクロールも不可。  
**原因:** v10を外したことでportal処理が消えたが、v18 CSSはbody直下前提のまま。  
**修正:** v19でbody直下へportalし、3段構造へ。  
**再発防止:** CSSセレクタ前提と実DOM階層をDevTools/コードで一致確認。

## M018: 放銃者/和了者選択画面まで点数表と同じ大きな幅
**症状:** 横に長く押しづらい。  
**原因:** non-score画面を一律サイズ化。  
**修正:** stepごとにwidthを分離。  
**再発防止:** score / winner / discarder / type は別レイアウト種別として扱う。

## M019: 下側プレイヤーが選択できない/overlayがタップを奪う
**症状:** winner/discarder選択で下プレイヤーが押せないケース。  
**原因:** overlay/pointer-events/riichi領域との競合。  
**修正:** capture phaseでplayer-panel clickをルーティング。  
**再発防止:** 4方向すべてを毎回テストし、リーチ領域を押した場合も確認。

## M020: 起家マークが和了/放銃マークと重なった
**症状:** 状態マークと「起」が重なる。  
**原因:** 同じアンカー/座標系に複数バッジを配置。  
**修正:** 起家マークを起家本人から見て右下へ独立配置。  
**再発防止:** 恒常マーク（起家）と一時状態マーク（和了/放銃）を別レイヤー・別アンカーにする。

## M021: カメラ追加後、メニュー最下部「閉じる」が見切れた
**原因:** メニュー項目増加に高さ設計が追従しなかった。  
**再発防止:** メニュー項目追加時は最小端末サイズで最下部操作まで確認。

---

# D. カメラ・MediaStream・負荷

## M022: 手牌確認後もカメラMediaStreamが生き残り、iPhoneがカメラ使用中になった
**症状:** iPhone上部が緑、スクショでシャッター音、ボタン反応が遅い、タスク切替時に一瞬カメラ画面が見える。  
**原因:** カメラのキャンセル経路ではstopしていたが、手牌確認→役判定へ進む経路でMediaStream tracksを確実にstopしていなかった。  
**修正:** 手牌確定/画面遷移/pagehide/visibility hiddenで全track.stop。  
**再発防止:** カメラ画面から出る全経路にstopMediaを置く。  
**回帰テスト:** カメラ終了後にiOS緑インジケータが消えること。

## M023: DOM監視が自分の更新を再検知し続け、v15で強いラグ
**症状:** ボタン反応遅延、画面が重い。  
**原因:** MutationObserverがclass/DOM変更を監視し、そのcallback自身がclass/DOMを書き換えて再発火。  
**再発防止:** observerは必要なmutationだけに限定。自分が書き換えるclass/characterDataを監視しない。可能ならユーザー操作イベントで明示更新。

## M024: 点数表の監視ループで白画面→PWA再起動風→途中入力復旧
**時期:** v23→v24。  
**症状:** 点数表に青/橙/緑が出た直後、しばらくすると白くなり「入力途中データがあります」で戻される。  
**原因:** 明示的な `location.reload()` は無かった。v8/v18のobserverが、自分で追加する推奨装飾/ボタンを再検知し続ける構造だったため、iPhone WebViewの負荷上昇要因になっていた。WebView再起動そのものの内部理由はOS側で完全には確定不能だが、自己再発火ループはコード上で確認済み。  
**修正:** v8はchildList/data-tileだけ、v18は点数表DOM新規追加だけを監視。推奨ボタンは毎回作り直さず再利用。  
**再発防止:** MutationObserver追加時は「callbackが自分の監視対象を変更していないか」を必ずレビュー。  
**回帰テスト:** 点数表を30秒以上放置し、白画面/再起動/CPU負荷が出ない。

---

# E. 手牌入力・カメラ結果

## M025: 連続牌選択で毎回1番スロットを上書き
**時期:** M8 v3初期。  
**症状:** 複数牌を選んでもslot1だけ変わる。  
**原因:** picker/title更新を契機にcurrent indexがリセット。  
**修正:** picker clickをcaptureし、pickerを開いたままcurrent slotだけ進める。  
**再発防止:** 連続入力UIでは表示更新と選択状態の初期化を分離。

## M026: 「読み取り直す」がライブカメラではなくゲーム画面へ戻る
**原因:** 戻り先を一般的なclose処理に任せた。  
**修正:** hand-resultの戻る操作をライブカメラ起動へ明示ルーティング。  
**再発防止:** 「戻る」の意味を各画面で仕様化し、単純な前画面復帰と混同しない。

---

# F. 符計算・面子・状態受け渡し

## M027: 通常形で符が出ず、七対子だけ25符・国士だけ「符なし」
**症状:** 通常アガリでは青の翻は出るが緑推奨が無い。  
**原因:** m8-v8は複数の符候補があると `m8SuggestedFuV8=null` にする仕様で、点数表側が単一推奨を作れなかった。  
**教訓:** 「計算できない」のではなく「複数候補」の場合をUIで解消する必要がある。  
**再発防止:** nullの意味を「失敗」と「複数候補」で分ける。候補選択UIか、追加情報取得で一意化。

## M028: v13で符表示を直したつもりでも、結果画面に符が出なかった
**原因:** 待ち候補までは取れていたが、実際の和了者/ロンツモ等の状態と符計算の接続が不完全。  
**再発防止:** 符計算の入力値（14枚、和了牌、門前/副露、ロン/ツモ、和了者、面子状態）を一覧化し、どの画面で確定するかを確認してから実装。

## M029: 「14枚あるのに0枚」
**時期:** v18。  
**症状:** 画面には14枚表示されるのに「手牌が14枚ではありません（0枚）」。  
**原因:** 表示DOMの14枚と符計算の参照元 `window.m8LastTilesV8` が別で、参照側が空。  
**修正:** v19で結果画面DOMから実際の14枚を読み、状態へ同期。  
**再発防止:** 同じ情報のSource of Truthを増やしすぎない。画面と計算が別データ源を使う場合は同期点を明示。

## M030: 旧符候補表示が残り、新しい確定符と矛盾して見えた
**例:** 上で50符確定なのに下に「50・60符候補」。  
**原因:** v20の新計算とv18の旧表示が同時に生きていた。  
**再発防止:** 新しい計算系を有効にする時、旧表示DOMをhide/removeし、ユーザーに複数の真実を見せない。

## M031: 副露ありだけでは正確な符が一意に決まらない
**原因:** 「副露あり」だけでは、どの刻子を鳴いたか、明刻/暗刻、明槓/暗槓が分からない。  
**修正:** チーのみ / ポン / 明カン / 暗カンを面子単位で指定。  
**再発防止:** ルール上必要な情報を「推測」で埋めない。足りない情報はUIで取得。

## M032: カンを物理4枚のまま14枚入力に入れようとすると牌数モデルが破綻
**教訓:** 論理手牌14枚では刻子3枚として扱い、別途「明カン/暗カン」を指定して符を加算する設計が必要。  
**再発防止:** 実牌枚数と和了形判定用の論理牌数を分離。

## M033: 符計算の原因を説明せず位置/表示だけ直し続け、修正が往復
**症状:** v13〜v18で「符が出ない」「中央にならない」修正が何度も往復。  
**原因:** その場の見た目修正を先にし、入力状態・DOM階層・計算データフローの根本原因を十分言語化しなかった。  
**再発防止:** ユーザー要望通り、修正前に必ず「症状→原因→どこを直すか」を言語化する。

---

# G. 役・翻・点数表連携

## M034: 役/符の新旧表示が同居してユーザー判断を迷わせる
**原因:** v5/v18/v20/v21等が別々に説明・候補表示を追加。  
**再発防止:** 同じ概念は最終的に1つの表示へ集約。新判定が確定したら旧候補を消す。

## M035: 点数表の青列・橙符行・緑推奨セルは「計算結果」ではなく一時DOM装飾だった
**症状:** 初回は出るが、ページ再起動/入力途中復旧後は消える。  
**原因:** core draftは `agariFlow`（step, winners, discarder, scoreResults等）しか保存せず、M8の14枚・和了牌・門前/副露・ポン/カン内訳・符・翻を保存していなかった。  
**修正:** v24でM8補助状態をlocalStorageへ保存し、途中入力復旧時に再注入して青/橙/緑を再装飾。  
**再発防止:** 「画面に見えている重要状態」がDOMだけに存在していないか確認。復旧対象は計算元データまで保存。  
**回帰テスト:** 点数表表示→再起動/復旧→青/橙/緑が同じ状態で戻る。

---

# H. PWA/保存・復旧

## M036: 対局本体の自動保存と入力途中保存が別系統で、M8状態が抜けた
**構造:** 
- 対局本体: `MahjongScoreApp_local_match_v1`
- 入力途中: `MahjongScoreApp_draft_v1`
- M8判定: 当初はwindow変数/DOM中心
**原因:** 後からM8を追加したため、保存設計に組み込まれていなかった。  
**再発防止:** 新しい状態変数を追加した時は「通常保存 / 入力途中復旧 / クラウド同期 / Undo」の4箇所に必要かを確認。

## M037: 開発時間の累計を会話上の部分記録から再計算し、12時間50分と誤答
**正しい値:** ユーザー確認済み累計は **23時間30分**。  
**原因:** 不完全な個別記録を足してしまった。  
**修正:** `docs/development-time.md` を唯一の正本にした。  
**再発防止:** 開発時間を答える/更新する前に必ず同ファイルを読む。過去セッションを再加算しない。

## M038: 作業記録/管理ファイル/旧ZIPと最新コードの整合性がずれるリスク
**原因:** 長期開発で複数の引継ぎファイル・ZIP・GitHub版が存在。  
**再発防止:** コードはGitHub mainを最新正本、開発時間はdevelopment-time.md、既知ミスは本ファイル。旧ZIPを最新とみなさない。

---

# I. Supabase/クラウド

## M039: SupabaseプロジェクトをSeoulで作成後、Tokyoで作り直し
**教訓:** リージョン選択は後で簡単に変えられない。  
**再発防止:** 現行TokyoプロジェクトのURL/設定を正本として扱い、旧環境を混ぜない。

## M040: フロントエンドへsecret/service_role keyを置く危険
**再発防止:** ブラウザへ置くのはpublishable/anon相当のみ。RLS前提。secret/service_roleは絶対に埋め込まない。

## M041: Realtime更新競合
**設計上のリスク:** 複数端末が同時更新すると上書き競合。  
**対策:** version条件付きupdate、0件更新なら最新版再取得。  
**再発防止:** クラウド関連修正時にこの競合制御を壊さない。

---

# J. テスト方法

## M042: 1個ずつテストさせ、毎回手牌を作り直す負担が大きかった
**ユーザー要望:** テスト系はまとめて一気に、同じ設定/手牌を使い回して連続実施したい。  
**再発防止:** テスト項目は可能な限り1セット化し、途中状態を保持して条件だけ切替える。

## M043: 修正ごとに局所確認だけで、前に直した不具合が再発
**例:** 中央配置、フッター、スクロール、下プレイヤー選択、カメラ停止など。  
**再発防止:** 変更カテゴリに応じて既知ミス回帰チェックを再実施する。全項目を毎回やる必要はないが、該当カテゴリは必須。

---


## M044: 和了者/放銃者選択の幅が、選んだ席方向で長くなったり短くなったりする
**時期:** M8 v24 → v25  
**症状:** 左/右プレイヤー選択時は正しい短い幅だが、上/下プレイヤーを選択すると選択モーダルが横長になる。  
**根本原因:** v18とv19が同じ `#agari-overlay` のwidth/positionを別々に担当していた。v18は非点数画面を最大680px・78%、v19はwinner/discarderを最大510px・56%に設定しており、プレイヤー選択でDOMが変わった際に最後に走った旧v18/v19の処理順によって幅が揺れた。方向そのものが原因ではなく、方向ごとの選択表示DOM更新をきっかけにしたレイアウト責務競合。  
**修正:** v19以降をアガリoverlayの唯一のレイアウト担当として宣言し、v18のcenterAgariはv19+存在時にwidth/positionを書き換えない。v25でもwinner/discarder時の正しい短い幅を操作後に再適用する。  
**再発防止:** 1つのoverlayのwidth/positionを複数世代のパッチで同時管理しない。旧版が計算/符処理のため残る場合も、レイアウト責務だけは明示的に無効化する。  
**回帰テスト:** ロン/ツモの和了者選択とロンの放銃者選択で、上→右→下→左を連続選択し、4方向すべて同じコンパクト幅を維持する。  
**確度:** 確定

## M045: 修正だけの版を先に出し、同じ版への次機能追加ルールを忘れた
**時期:** M8 v27
**症状:** ロン/ツモ初回中央配置と起家マークの修正だけで v27 として公開し、予定していた次機能の同時実装を省いた。
**根本原因:** ユーザー指定の「バグ修正＋次に実装する機能を同じバージョンで行う」を変更前チェックで最後まで実施していなかった。
**修正:** v27 の版数を変えずに、点数表で入力手牌枚数・和了牌・門前/副露・符・翻・面子指定を展開して確認できる機能を追加。
**再発防止:** 新しいバージョンの完成条件は「修正コミット＋予定機能コミット＋読込確認」。修正だけの公開は原則完了報告しない。
**回帰テスト:** v27 の初回ロン/ツモ中央配置・起家マーク右下・4方向プレイヤー選択と、点数表の「判定に使った情報を確認」を同じ対局で連続確認する。
**確度:** 確定

## M046: アガリ方法画面だけ初回に上へずれ、戻る操作後に中央に来る
**時期:** M8 v27
**症状:** 「アガリ」最初のロン/ツモ画面だけ上部に出る。和了者選択は中央で、そこから「戻る」するとロン/ツモも中央。
**根本原因:** v27の中央配置処理は `agariFlow.step==='method'` を条件としていたが、実際の `script.js` のアガリ方法画面のstepは `'type'`。条件が成立せず初回用クラスが付かなかった。
**修正:** 中央配置条件を `step==='type'` に修正。visualViewportの実座標から画面中央を計算し、DOMクラス切替時に一度だけ適用。v27の判定入力情報レビュー機能も同じ公開版に含める。
**再発防止:** UIのステップ識別子は推測で命名しない。対象 `renderAgariFlow` と実際のstep代入値を確認してから分岐を実装。旧パッチが同じ画面を触る場合は処理競合も調べる。
**回帰テスト:** 最初のアガリ→ロン/ツモが中央、ロン→和了者（4方向反応）→戻る→ロン/ツモが中央。点数表の「判定に使った情報を確認」も同一対局で確認。
**確度:** コード上のstep不一致は確定。iPhoneでの最終表示は実機確認待ち。

## M047: 判定情報パネルを開くとiPhone PWAが再起動・繰り返しクラッシュ
**時期:** M8 v29 → v30
**症状:** 点数表の「判定に使った情報を確認」を開くと白画面→入力途中データの復旧。再度開くとSafariの「問題が繰り返し起きました」表示。
**コード上で確認した問題:** v27は `#agari-overlay` の子孫すべてのchildList/class変更を常時監視し、通知のたびに次フレームで配置・判定情報DOM更新を行っていた。v28/v29は同じdetails内へ別々に大きなフォームを追加していた。多数の旧版レイアウト・符表示処理も同時に稼働し、過去のM023/M024と同系統の多重DOM処理・再発火リスクを再導入していた。
**原因の確度:** このDOM監視とフォーム多重挿入はコード上で確定。iOS WebKitのクラッシュ内部原因まではログ未採取のため未確定であり、単独原因と断定しない。
**修正:** v27/v28/v29をindex.htmlの読込からまとめて外し、必要な機能（初回中央配置・起家マーク右下・判定情報表示・実卓照合）をv30の単一モジュールへ統合。子孫監視を廃止し、overlay自身のclass変更だけを監視。判定フォームはユーザーが詳細を開いたときに一度だけ作り、表示中の自動DOM再構築を行わない。
**同時追加した次機能:** 実卓で入力した比較結果の最後の1件を端末に保存し、「前回の結果」から再表示・コピーできるようにした。
**再発防止:** scoreレビュー領域を増築する度に新observer/旧パッチを積まない。追加フォームは1つのmoduleで管理し、子孫DOM監視や頻繁な再生成を行わない。WebKitが落ちたら「発生画面」「前後の操作」「既存observer」「新規作成DOM」を確認し、実機確認前は「修正確認済み」と断定しない。
**回帰テスト:** 同じ手牌の点数表で判定情報を開く→値を表示→比較を保存→前回を読み込み→折りたたみ/再展開→30秒放置してもPWAが再起動しない。途中保存復旧後も青/橙/緑が残る。画面上部のロン/ツモとプレイヤー選択に影響しない。
**確度:** 監視構造の問題と修正はコード上で確認済み。実機安定性はユーザーのiPhone検証待ち。

## M048: 入力途中の復旧後に点数表の案内が巨大化し、再び白画面になる
**時期:** M8 v30 → v31
**症状:** 点数表表示後に一度白画面→入力途中データの復旧。復旧後の点数表では「M8総合判定」「M8推奨」「M8符判定」などが縦に重なり、表本体が下へ追いやられて操作しづらい。
**コード上で確定した問題:** v19はbody全体のchildList変更すべてに対してアガリ画面全体の再レイアウト・14枚同期を実施していたため、v30の判定詳細UIが追加された際にも再実行された。v12はbody全体のchildList/characterData変更を監視して符候補の再解析をしていた。採点表より上に旧v18の符案内・自動入力ボタン、v21の符案内・入力ボタン、v25の総合案内などが積み重なり、iPhone横画面では点数表本体の視認領域が減っていた。
**原因の確度:** 過度に広いDOM監視と重複案内表示はコード・画像上で確認済み。iOS WebKitの白画面の単独直接原因についてはブラウザ内部ログがないため確定していない。
**修正:** v19のDOM監視を新しい画面/点数表DOM追加だけに限定、v12の符候補監視を結果画面内部に限定。v31のCSSで重複する旧v18の符案内/入力ボタンを非表示にし、v19の3段固定レイアウトと本文縦スクロール、最下部の操作ボタンを維持する。点数表の符・翻・緑枠および実際の計算処理は変更しない。
**同時実装した次機能:** 実卓との符/翻比較メモを端末内に直近3件まで保存し、「直近3件の履歴」からまとめてコピー可能にした。v30の前回1件保存も引き続き利用する。
**再発防止:** 点数表へ案内を追加する前に既存の情報表示と重複しないか調べる。observerをbody全体に張って再装飾しない。横画面の実表示高さを基準に、表本体の表示面積/スクロール/フッターを確認し、実機の安定性をテストするまで「クラッシュ修正済み」と断定しない。
**回帰テスト:** 同じ入力途中状態で点数表を開く→緑枠/符/翻を確認→判定情報詳細を開閉→比較結果3件を保存/表示→上下にスクロールして表最下段・固定フッターを操作→30秒放置→タスクキル復旧後に同じ表サイズ・青/橙/緑を確認。
**確度:** 対象コード修正とレイアウトの競合減少はコード上で確認。iPhoneでの白画面再発有無は実機確認待ち。

## M049: 自分で追加した翻数ガイドで全画面DOM監視が再発火する
**時期:** v31自動安定化作業
**症状:** 点数表を開いて情報欄を更新すると大量のDOM変化が起き、iPhoneで白画面が発生しうる。
**根本原因:** v6の `decorateScoreTable` はガイドを必ずremove→createし、body全体のchildList/characterDataを監視するObserverがその変更を再検知して再描画する構造。v7・v9の旧レイアウト監視/scale適用もv19の表示処理と競合していた。
**修正:** v6のガイドを再利用し、Observerは新しい点数表の追加のみを検知。v7旧配置Observerを停止し、v9旧配置クラスの付与を停止。v19をアガリ画面の配置担当とする。点数表の点数計算、既存の符・翻の保存は変更しない。
**再発防止:** observerが自分の更新対象を監視していないことをレビューチェック。新規UIの追加時は古いレイアウトやガイドが再作成されないことを静的検証・ブラウザで確認。
**回帰テスト:** Chromium横画面で対局開始→ロン→和了者・放銃者→点数表→判定情報欄を開閉。数秒放置後もDOMノード数が増殖しないことと配置が画面内に収まることを自動チェック。iPhone PWAでの完全な安定性は未検証。
**確度:** コード上の再発火経路は確定。iOS上のクラッシュ直接原因としての寄与度は未確定。

## M050: 一盃口を4順子限定で判定していた
**時期:** v31以前
**症状:** 3順子＋1刻子の和了形で同じ順子を2面子含んでいても、一盃口が検出されない。
**根本原因:** v22の一盃口と二盃口の判定を一括して `seqs.length===4` の内側で行っていた。
**修正:** 同順子2面子なら残り面子が刻子でも門前一盃口を判定。二盃口は順子4面子で同順子2ペアが揃う場合に限定。純粋関数化してテストで両方確認する。
**再発防止:** 役の検出は成立条件と非成立条件の両方を単体テストする。対象役の成立条件にない「全順子」などを誤って付け足さない。
**回帰テスト:** 門前の3順子+1刻子で一盃口、門前の4順子で二盃口、副露時はどちらも成立しない。符の刻子加符と切り上げも単体テスト。
**確度:** コード上の条件不備は確定。

## M051: 実牌が14枚あるのに候補0枚で撮影確認ボタンが無反応、キャンセルと重なる
**時期:** M7 v33→v34
**症状:** iPhone横画面の実牌14枚を映しても「0/14」のまま。「この瞬間を確認」を押しても反応しない。右下の確認/キャンセルボタンが重なっている。
**根本原因:** v33の確認ボタンは `if(lastBoxes.length)` が成立する時しか動作しない設計だった。牌が隙間なく並ぶ実物では明るい牌面がひとかたまりになり、連結成分を14個に分離できず0件になることがある。ボタンは同じ `right:14px;bottom:12px` と旧版 `right:16px;bottom:14px` に配置され、重なっていた。画像で0件を確認、連結成分が直接原因かは実機ログ未取得のため有力仮説。
**修正と次機能（同時公開）:** ボタンを左下（確認）と右下（キャンセル）に分離し、safe-areaとz-indexを統一。撮影画像が取得できれば候補0件でも必ず確認画面へ進める。自動で14個に分けられなかった場合は画像を幅14等分した候補プレビューを全て「?」として表示し、実牌を見ながら牌名を手動指定できるようにする。検出結果と手動分割を混同させない。
**再発防止:** 「認識0件」を撮影確認UIの無反応にしない。実機上の操作ボタンは旧CSSと重ねて全セレクタを確認する。認識の不確かさを実際より良く見せず、手動入力へ安全に移行させる。
**回帰テスト:** Chromium横画面で候補0枚を合成し、「この瞬間を確認」→14個の未確定スロット表示と編集可能性をチェック。確認・キャンセルの矩形非重複を確認。同じ実牌で0枚検出でも確認画面へ進み、14枚修正・確定後にカメラ使用緑表示が消えるかiPhone実機で確認。
**確度:** ガードによるボタン無反応・座標競合はコード上で確定。候補0件となる物理的な分割失敗は有力。iOS実機の修正結果は最終確認待ち。

## M052: 0枚時の手動14分割が牌の列ではなく撮影画面全体を縦切りしていた
**時期:** M7 v34→v35
**症状:** iPhoneで実牌14枚を横並びにした写真から確認画面へ進むと、14スロットには牌の一部ではなく木目の机・周囲の物体が縦長に表示される。? のままでは正しい牌を選びにくい。
**根本原因:** v34の `manualGuideBoxes` は撮影canvas全幅・全高の72%を固定14等分し、写っている牌列の位置と高さを求めていなかった。連結成分方式は隙間のない実牌列を別の牌に分離できず、手動フォールバックが頻出していた。
**修正と次機能（同時実装）:** 撮影画像の輝度・低彩度の横帯から牌列のおおよその上下左右を探し、その帯だけを14等分して牌画像プレビューを作る。帯が確実に求められない場合は、無関係な14枚の断片を表示せず、元画像全体＋14個の手動選択欄を表示する。推定された牌列を利用し、ユーザーが14枚の牌名を確定した場合だけローカル認識用特徴を学習する。牌種を「自動認識した」と偽らない。
**再発防止:** 撮影画面全体を牌位置とみなさない。ユーザー提示の画像から実際にどこが牌列かを確認。横一列・隙間なし/机の背景/複数色を含む合成ブラウザテストを追加する。推定できないときは画像全体を使えるようにして手動選択を妨げない。
**回帰テスト:** 実牌14枚を映して確認→プレビューに机ばかりではなく牌面14枚が並ぶ、または元画像全体を表示し14枚を手動指定できる。読み取り直し・カメラ終了・端末緑インジケーター消灯。横画面合成テストで明るい牌列の座標を推定し、14枠のyと高さが牌列に収まることを検査。
**確度:** 全画面を固定縦割りするバグはコード・画像から確定。実牌上での牌列推定精度はiPhone最終チェック待ち。

## M053: 実牌スケールと合わない個別牌フィルタに依存し、0/14を招いた
**時期:** M7 v35→v36
**症状:** iPhone実機で白枠内に14枚を横一列に収めても候補が `0/14` のまま。確認画面では牌列推定にも失敗し、元画像＋未確定14枠へフォールバックした。
**根本原因:** v35のライブ検出は、画面全体を480×190へ縮小したうえで明るい連結成分を「個別牌」として探し、`bh < h*.34` の候補を除外していた。ユーザー実機スクリーンショット上の牌高はカメラ表示高さの34%より明確に小さく、この条件と実際の撮影スケールが噛み合っていない。また、密着した牌は明るい領域が一体化しやすく、個別14成分を前提にする方式自体が不利だった。正確な実フレーム内の各連結成分寸法はログ未取得。
**修正と次機能（同時実装）:** M7 v36ではライブ中に14個の個別候補を成立条件にしない。固定の手牌枠へ14枚を入れてユーザーがシャッターを押し、`object-fit: cover` を考慮して白枠に対応する元動画の高解像度領域を切り出す。その白枠内の低解像度コピーで横一列の牌帯を探し、高解像度側の同じ範囲を14分割して確認画面へ渡す。実牌スクリーンショットが旧固定輝度条件より暗かったため、牌列探索は固定RGB閾値から「輝度に対する色差比」を使う相対的な中性色判定へ変更し、暗めの牌面も候補に残す。誤認識・未認識は既存のタップ修正を使う。牌列を見つけられない場合は白枠内の撮影画像を表示し、偽の14分割を出さない。固定枠を実牌列に合わせて細くし、シャッターを中央へ配置。`visibilitychange` で非表示になった場合も既存キャンセル経路を呼び、MediaStream停止を優先する。PWAキャッシュ名をv36へ更新し、v36カメラ/認識コアもプリキャッシュ対象に追加した。
**再発防止:** 画像認識条件は合成画像だけで決めず、実際の撮影スケールを基準にする。ライブ候補数をUX上の必須ゲートにしない。位置検出用の低解像度画像と牌種確認用の高解像度画像を分け、撮影範囲と元動画座標の変換を自動テストする。
**回帰テスト:** Chromium横画面で固定白枠のシャッターボタンとキャンセルの非重複、`object-fit: cover` の元動画座標変換、密着した長い白色牌列の検出、14分割、14個の高解像度プレビュー生成を確認。iPhone PWAでは実牌14枚を一度だけ並べ、撮影→14プレビュー→誤り修正→読み取り直し→カメラ緑表示消灯を一括確認する。
**確度:** 個別牌高さフィルタと実機表示スケールの不整合はコード＋実機スクリーンショットから確認。密着牌の連結成分化も設計上の有力要因。各実フレームの内部ログによる寄与率は未確認。

## M054: 14分割には成功したが確認画面が細い縦切り表示になり、初回学習も保存前に画面が閉じていた
**時期:** M7 v36
**症状:** iPhone実機で白枠内の牌列は14枚に分割できたが、確認画面では各牌が細長い縦ストリップのように見え、`0 / 14枚を自動候補化` のままだった。
**根本原因:** 実写プレビューを縦長の14列ボタンへ `background-size: cover` で表示していたため、各牌画像の中央部分だけが拡大表示されていた。さらに学習保存のdocument click listenerが通常のbubble phaseで動いており、`この手牌で進む` の既存 `onclick` が結果overlayを先に閉じた後ではDOMから14ラベルを読めない経路があった。
**修正:** 実写プレビューを `background-size: contain` と有限高さへ変更し、牌全体を表示。学習保存listenerをcapture phaseへ移し、結果画面が閉じる前に14枚の検証済みラベルと特徴量をlocalStorageへ保存する。学習データが空の初回は「初回学習」と明示し、今回は14枚を手動指定すれば次回候補に使われることを案内する。公開asset queryとPWA cacheも更新。
**再発防止:** 画像の切り出し成功とUI表示成功を別テストにする。ユーザー確認後の学習・保存処理は、画面を閉じるhandlerとのイベント順序を自動テストする。初回学習データ0件を「認識失敗」と誤解させない。
**回帰テスト:** Chromium横画面で撮影→14実写プレビュー→computed styleのbackground-sizeがcontain→14ラベル付与→確定クリック→overlayが閉じても14種類の学習テンプレートがlocalStorageに残ることを確認。その後通常の対局開始〜採点画面フローも通す。
**確度:** プレビュー表示原因・学習イベント順序ともコードと実機表示から確定。自動テストで修正済み。iPhoneでの学習後の次回自動候補精度は実機確認待ち。

## M055: 2回学習後の3回目撮影で自動候補が6/14から0/14へ落ちた
**時期:** M7 v36
**症状:** 初回14枚を手動確定→2回目は6/14を自動候補化→残りを修正して再学習した後、3回目の同じ手牌撮影で0/14へ低下した。14分割自体は継続して成功。
**根本原因:** 学習テンプレート消失ではなく、現在の96要素（8×12）グレースケール特徴が、撮影ごとの牌位置・切り出し位置の小さなズレに敏感だった。テンプレート追加は同一ラベル内の最小距離を悪化させないため、6→0の低下は撮影3回目の特徴が既存テンプレートから閾値外へずれたことを示す。実機の各距離値は今回未取得だが、コード上の特徴表現は位置ずれ不変ではない。
**修正:** 96要素特徴同士の比較時だけ、8×12格子を上下左右±1セルずらした複数比較の最小RMS距離を採用する。ずらしには小さなペナルティを入れ、既存の判定閾値0.24・競合ラベルmargin 0.035は維持して誤認識を増やしすぎない。既存の端末内学習データ形式は変更しないため、再学習のやり直しは不要。各牌の上位3候補距離も診断情報へ保存する。
**再発防止:** テンプレート画像比較では、明るさ正規化だけでなく小さな平行移動に対するロバスト性を単体テストする。認識率が上下したときに同じ手牌の再学習だけをユーザーへ繰り返し求めず、特徴距離と切り出しずれを先に確認する。
**回帰テスト:** 8×12の特徴を1セル横へずらした合成データで、通常RMSは閾値超え、shift-tolerant RMSは正解ラベルを近距離として選べることを確認。全既存役・符・カメラ・Chromium横画面テストも成功。
**確度:** 特徴表現が位置ずれに弱いことはコード上で確定。今回の実機0/14への寄与は有力。修正後のiPhone認識率は実機確認待ち。

## M056: v37のシーム補正で分割境界がむしろ不安定になり、自動候補が0/14へ低下した
**時期:** M7 v37→v38
**症状:** 同じ14枚の比較で、旧v36画面は1/14、最新v37画面は0/14。v37では複数の切り出し画像に隣接牌や木目が入り、末尾側でも牌面が枠から外れる箇所が見えた。
**根本原因:** v37で追加した「縦エッジ＋白牌面の少なさ」によるシーム探索が、牌間境界だけでなく牌模様そのものの強い縦線も拾い得た。実機では内部境界を誤って移動させ、均等分割より不安定なcropを作る回帰になった。
**修正:** v38ではシーム補正を認識経路から外し、実機で相対的に安定していた牌列全体の14等分へ戻す。代わりに分類側を変更し、牌面正規化後の画像から12×18の「インク量」特徴（背景輝度に対する暗さ＋色差）を生成して照明差の影響を減らす。216次元特徴は±1セルのshift-tolerant比較を使用。旧特徴は混在させず新しいlocalStorageキーへ分離。
**再発防止:** 境界推定を改善する変更は、認識率だけでなく「各cropに1牌だけが入っているか」を実機画像で確認する。合成シームテストだけで採用しない。入力cropが安定している場合は、まず分類特徴を改善し、境界処理を過度に複雑化しない。
**回帰テスト:** Chromiumで同一記号を明るさの違う牌面に描いた2画像と別記号1画像を作り、v38の特徴で「照明差のある同一記号」の距離が「別記号」より小さくなることを確認。既存の固定枠撮影・14分割・学習保存・対局/採点フローもCI成功。
**確度:** v37のcrop回帰はユーザー実機スクリーンショットで確認。v38分類の実牌改善量はiPhone確認待ち。

## M057: v38の自動候補8枚のうち4枚が誤認識で、候補数を増やすほど修正負担も増えた
**時期:** M7 v38→v39
**症状:** 14枚を並び替えた実機テストで8/14枚を自動候補化したが、ユーザー確認では正解4枚・誤り4枚。候補のprecisionは50%で、未認識「?」より誤った自動入力の方が確認負担を増やした。
**根本原因:** v38の最近傍テンプレート判定は絶対距離0.30以内かつ2位との差0.012以上で自動採用しており、1〜数枚の学習テンプレートしかない段階としては受理条件が緩かった。特徴量そのものにも改善余地は残るため、4件すべての誤認識を閾値だけが原因と断定はしないが、「曖昧でも自動確定する」判定ロジックはコード上の確定問題。
**修正と次機能:** v39では「誤候補を出さない」precision優先へ変更。最良候補の絶対距離、2位との差、1位/2位距離比をすべて満たす高信頼候補だけ自動入力し、それ以外は「?」のままにする。同時に、?や修正対象をタップした牌選択画面へ上位3候補をショートカット表示し、保守的に自動入力を減らしても手修正のタップ数が増えすぎないようにする。既存v38学習ライブラリはそのまま利用する。
**再発防止:** 認識性能は「何枚候補が出たか」だけで評価せず、候補precision（正解候補/自動候補）を必ず記録する。誤候補が多い段階ではrecallよりprecisionを優先し、曖昧な牌は未確定として人に返す。
**回帰テスト:** Chromiumで明確な1位候補は採用、1位/2位が近い曖昧候補と絶対距離が遠い候補は棄却することを確認。結果画面の牌をタップすると上位3候補のショートカットが表示されること、初回学習保存・固定枠撮影・通常対局/採点フローも通ることを確認。
**確度:** 実機で8候補中4誤りは確定。受理条件が緩いことはコード上確定。特徴量自体の誤り寄与は今後の実機結果で継続評価。

## M058: 学習テンプレート追加後にv39の順位品質が悪化し、連続入力の近い候補も前の牌のまま残った
**時期:** M7 v39→v40
**症状:** 追加学習後の別順序実機テストで高信頼候補6枚のうち正解2枚（precision 33%）。未確定12枚のTop3に正解があったのは4枚（約33%）で、直前テストのTop3 11/13から悪化。また「近い候補」を押して次の牌へ自動移動した際、候補欄が前の牌のTop3のまま残り、一度閉じないと次牌の候補を確認できなかった。
**根本原因:** 認識順位は各ラベルの複数テンプレートのうち最小距離1件だけで評価していた。学習データが増えるほど、誤ラベル側にも偶然近い1テンプレートが生まれやすくなり、正しいラベルの一貫性を評価できなかった。候補UIは結果牌を最初にタップした時だけ描画し、連続入力でactive indexが進んだ際の再描画処理がなかった。
**修正:** v40ではラベルごとに近い最大3テンプレートの平均距離を使うrobust rankingを追加し、「偶然1枚だけ近い誤ラベル」を抑える。単一テンプレートのラベルにはpenaltyを付与。自動確定はrobust scoreとbest raw distanceの両方で保守的に判定する。候補UIは現在選択中の牌indexを追跡し、近い候補または通常一覧から選択して次の牌へ自動移動した直後にTop3を自動更新する。
**再発防止:** 学習データを増やした時に最小距離だけで分類精度が上がると仮定しない。複数サンプルがある場合はラベル内一貫性を評価する。連続入力UIは「最初に開いた状態」だけでなく、自動advance後の表示内容まで回帰テストする。
**回帰テスト:** pure coreで「誤ラベルに偶然1件だけ非常に近いテンプレートがある」ケースを作り、nearest-minでは誤ラベル、robust multi-templateでは正ラベルになることを確認。Chromiumで1枚目Top3表示後に2枚目indexへ進めると、2枚目固有のTop3へ置き換わることを確認。既存カメラ/学習/対局/採点フローも全成功。
**確度:** v39の6候補中2正解・Top3 4/12と候補UI staleは実機確認済み。最小距離方式の構造的問題とUI再描画欠如はコード上確定。v40 robust rankingの実牌改善度はiPhone確認待ち。

## M059: v40で通常の牌一覧から修正した時だけ次牌の近い候補が更新されなかった
**時期:** M7 v40→v41
**症状:** iPhone実機で「近い候補」から選んだ場合は次の牌へ進むとTop3も次牌用へ更新されたが、通常の34牌一覧から修正した場合は次牌へ進んでも前牌のTop3が残った。
**根本原因:** v40はpicker内クリック後30msで一度だけcurrent indexを読み直す実装だった。実機の連続入力UIでは通常一覧選択後のactive index更新がその読み直しより遅い経路があり、更新前のindexを再描画していた。一方、近い候補ボタン側は明示的に次indexへfallbackしていたため症状が出にくかった。
**修正:** v41ではpicker title（「N枚目を選択中/修正」）のMutationObserverと、0/35/90/170/300msの段階的同期を併用。picker内のどの入力経路でもactive indexが変わった時点で次牌のTop3候補へ再描画する。suggestion領域自身の変更は監視対象にせず、自己再発火ループを避ける。
**再発防止:** 連続入力UIはショートカット経路と通常一覧経路を別々に実機/回帰確認する。非同期UIの状態更新を固定30ms一発で決め打ちしない。MutationObserverは状態源だけに限定して監視し、描画領域全体をsubtree監視しない。
**回帰テスト:** Chromiumで1枚目pickerを開いた後、titleを「2枚目を選択中」へ変更するとobserverだけでTop3が2枚目固有候補へ更新されることを確認。既存カメラ・学習・対局・採点フローも全成功。
**確度:** 実機症状は確定。固定30ms同期の競合はコード上確定。v41のiPhone実機で通常一覧経路が直ったかは次回確認待ち。

## M060: v41でも近い候補が1枚目のまま残った—監視対象が実際のcurrent状態ではなかった
**時期:** M7 v41→v42
**症状:** iPhone実機で、近い候補から選んでも通常34牌一覧から選んでも、2枚目以降へ自動移動した後の「近い候補」が1枚目用のまま残った。
**根本原因:** 連続入力の現在位置は `m8-v3.js` 内のclosure変数 `current` が正本だった。v41は `.tile-picker-title-m7v5` の変化を監視していたが、`m8-v3.js` はtitleを常に「手牌を連続入力」に固定し、実際に変化する「N枚目を選択中」は `.m8v31-help` に表示していた。そのためv41のobserverはcurrent変更を検知できなかった。さらにM7側は正本currentへ直接アクセスできなかった。
**修正:** v42では連続入力の所有者である `m8-v3.js` が `picker.dataset.m8v31Current` にcurrent indexを書き出し、redrawごとに `m8v31-current-change` custom eventを発火する。M7側はこのowner-provided index/eventを直接購読し、次牌へ進んだ瞬間にその牌のTop3へ再描画する。help textはfallbackにのみ使用。
**再発防止:** 別モジュールのUI状態を表示テキストから推測しない。状態の所有モジュールからdataset/event/APIとして明示的に公開する。UI連携テストは実際の通常牌ボタンクリック→owner current advance→候補更新まで通す。
**回帰テスト:** Chromiumで1枚目のpickerを開き、通常34牌一覧のボタンをクリック。m8-v3のcurrentが1（2枚目）へ進み、M7のTop3が2枚目固有候補へ自動更新されることを確認。全CI成功。
**確度:** 根本原因はコード上確定。v42の実機修正確認は次回iPhoneチェック待ち。

## M061: インク量中心のfeatureでは並び替え後のTop3順位が4/13まで低下した
**時期:** M7 v40〜v42→v43
**症状:** UI修正後も牌種認識の実機性能は、高信頼候補1/1正解に対し未確定13枚のTop3に正解が4枚。precisionを守るため自動確定を絞ると、ランキング自体の弱さが修正負担として残った。
**根本原因:** v38〜v42のfeatureは正規化牌面を主に「どこにどれだけインクがあるか」のグリッド値へ圧縮していた。線の方向・輪郭形状を明示的に持たず、似た密度分布の牌を区別する情報が不足していた。閾値・nearest/robust rankingを変えてもTop3品質が安定しなかったため、分類閾値だけではなくdescriptor自体の情報不足が主要課題と判断。
**修正:** v43ではHOG（線方向・形状）60%、空間色情報（赤/緑/暗さ）25%、粗いインク占有15%を組み合わせた構造化featureへ変更。各成分は小さな位置ずれを許容して比較する。feature形式を新キーへ分離し、旧featureを混ぜない。
**将来対策:** 正解確定時にfeatureだけでなく正規化牌画像もIndexedDBへ保存する。今後descriptorやAI分類器へ移行しても、保存画像から新featureを再生成できるようにし、ユーザーへ毎回14枚再学習を要求しない。
**再発防止:** 認識改善で閾値だけを繰り返し調整しない。Top1 precision、Top3 hit rate、descriptorの情報量を分けて評価する。feature形式を変える場合はversioned storage keyとraw image再構築経路を用意する。
**回帰テスト:** HOG/色/インクの構造化距離、照明差同一形状<別形状、赤/緑差、14枚feature保存、IndexedDBへ14枚raw normalized image保存、既存カメラ/対局フローをChromiumで確認。
**確度:** v40実機Top3 4/13は確定。旧descriptorが形状方向情報を持たないことはコード上確定。v43の実牌改善量はiPhone確認待ち。

## M062: Top3命中率を認識精度の主指標として扱うと候補集合が小さい時に過大評価する
**時期:** M7 v39〜v43→v44
**症状:** Top3に正解が含まれる割合を改善指標として使っていたが、学習済み牌種が少ない段階では3候補を出すだけで偶然正解を含みやすく、実際のTop1識別能力を正しく表さない。
**根本原因:** 評価時に「候補数3」という固定幅だけを見て、母集団である学習済み牌種数を併記していなかった。候補集合が5種類ならランダムでもTop3命中率60%、10種類なら30%になり得るため、Top3単独では比較指標として弱い。
**修正:** v44以降の主指標は (1)高信頼自動確定precision、(2)14枚中の正しい自動確定数、(3)未確定牌のTop1正解率、(4)学習済み牌種数 とする。Top3は入力補助UIとして残すが、精度評価の主指標にしない。結果画面にも学習済み牌種数を表示し、候補欄は「参考・左から1位→3位」と明示する。
**次機能:** v43で保存した正規化牌画像を利用し、v44ではHOG集約特徴ではなく牌画像そのものの濃淡・輪郭・赤/緑分布を直接比較する。上下左右1px、±2度、±3%拡大縮小を許容して最小距離を求める。
**再発防止:** TopKを報告する時は必ず候補集合サイズとTop1を併記する。候補集合が小さい開発初期にTopKだけで前進判定しない。
**回帰テスト:** direct image matcherが小位置ずれを同一牌として別形状より近く評価すること、結果画面に学習済み牌種数が表示されること、候補UIが順位順を明記することを確認。
**確度:** Top3指標の過大評価可能性は数学的に確定。v44 direct matchingの実牌効果はiPhone確認待ち。

## M063: v44の直接画像比較は学習済み11種類でもTop1 3/14・高信頼0枚
**時期:** M7 v44→v45
**症状:** iPhone実機で学習済み11種類、高信頼候補0枚、未確定14枚のTop1正解は3/14。直接画像比較へ切り替えても実用水準に届かなかった。
**確定事項:** v44の比較は、各牌を等幅分割した後に軸平行の矩形で牌面を正規化し、±1px・±2度・±3%の小変換だけを許容していた。牌を並び替えると同じ実牌でも列内の位置が変わり、撮影角度・傾き・見かけの縦横比が変わるが、その幾何差を学習画像側/撮影画像側で共通の姿勢へ揃える処理はなかった。
**有力な原因:** v44の低Top1は、分類式そのものだけでなく、比較前の幾何正規化不足が大きく寄与している可能性が高い。ただし3/14の全誤りが姿勢差だけで説明できるとはまだ断定しない。
**修正と次機能:** v45では各等幅crop内で背景との差または明るい低彩度領域から中央の牌面成分を取り出し、PCAで長軸方向を推定。中心・回転・縦横サイズを共通姿勢へ正規化してから直接画像比較する。v43/v44でIndexedDBへ保存済みの画像も同じ正規化を再適用してv45特徴へ自動移行する。さらに未確定牌の中央を大きな「?」で隠すUIをやめ、右上の小さい「?」＋下部の「1位候補」を直接表示し、14回タップせずTop1評価できるようにする。
**再発防止:** 分類器を変更する前に、同一実牌を列の左/中央/右へ移した入力が共通姿勢に正規化されるかを独立テストする。crop生成と分類器を別々の問題として評価する。
**回帰テスト:** Chromiumで同じ模様の牌を0度/6度で描画し、v45正規化後は別模様より同一牌の距離が小さいことを確認。結果画面で未確定牌の画像中央が隠れず、1位候補表示用DOMを持つことも確認。既存の14枚切り出し・連続修正・対局/採点フローを継続確認。
**確度:** v44実機結果は確定。幾何正規化不足はコード上確定。実機精度への寄与率はv45確認待ち。

## M064: v45初期実装で同じ牌を1回の撮影中に3回姿勢正規化し、結果表示がテスト待ち時間を超えた
**時期:** M7 v45開発中（公開前）
**症状:** Chromium統合テストで牌列解析自体は完了したが、220ms後にも結果画面14枠がまだ開かず失敗した。
**根本原因:** 1牌ごとに `featureFromBox`、`cropDataUrl`、`trainingImageDataUrl` がそれぞれ独立に姿勢正規化を実行し、14牌で重い連結成分/PCA処理を3重に行っていた。非同期click handler中の同期画像処理がテストtimerより長くevent loopを占有した。
**修正:** `analyzeTileBox` を追加し、1牌につき96×144のcanonical imageを1回だけ生成。その1枚からfeature・表示crop・学習画像を派生させ、正規化処理を約1/3へ削減。
**再発防止:** 同一フレームからUI表示用/学習用/認識用データを作る場合、重い前処理結果を共有する。新しい画像前処理ではChromium実フローまで通してから公開する。
**回帰テスト:** 最適化後のbranch run 36164108644でpure testとChromium横画面フローが全成功。
**確度:** 原因・修正ともコード上およびCIで確定。

## M065: v45の姿勢補正は回転・大きさだけで、横位置による台形歪みを補正していなかった
**時期:** M7 v45→v46
**症状:** 同じ物理牌を同じ並びで連続撮影するとTop1 11/14だったが、同じ14枚を並び替えると8/14へ低下。高信頼は0枚。
**根本原因:** v45の正規化は白い牌面成分から中心・回転角・縦横サイズを推定してaffine変換していた。これは回転とスケールには対応するが、カメラに対して斜め位置へ移動した時の四辺の収束・台形歪み（projective distortion）は補正できない。実機差と整合するため、v46で射影補正を直接検証する。
**修正:** v46では牌面成分の境界を複数帯でサンプリングし、左/右/上/下の4辺を線形近似して四隅を推定。四隅からhomographyを解き、各牌を正面矩形へ射影変換してから既存の濃淡・輪郭・赤/緑直接比較へ渡す。四隅推定に失敗した場合はv45のaffine正規化へ自動fallbackする。
**可視化:** 結果画面に「射影 X/14」を表示し、14枚中何枚で射影補正を使えたか実機で確認できるようにした。Top1表示・高信頼判定・連続修正UIは維持。
**再発防止:** 同一配置の連続撮影だけで画像認識改善を判断しない。必ず牌の横位置を変えた並び替え条件でTop1を測る。補正方式を変更する場合は、その補正が実際に何枚へ適用されたか可視化する。
**回帰テスト:** Chromiumで既知の台形牌面から四隅を検出し、射影後が縦長の正面牌面になることとfeature kindがperspective-direct-v1になることを確認。既存14枚撮影・学習・修正候補・通常対局/採点フローも成功。
**確度:** v45のaffine正規化が射影変形を扱わないことはコード上確定。並び替え低下の主因が射影歪みかどうかはv46のiPhone実機結果で判定する。

## M066: v46公開時にv45の学習済み11種類を引き継げず、毎回「初回学習」になった
**時期:** M7 v46→v47
**症状:** iPhone実機でv46を1回目・2回目と別順序で撮影しても、どちらも「初回学習：14枚を指定してください」「保存済みの牌画像がないため」と表示され、Top1候補を評価できなかった。
**根本原因:** v46は新しいfeature keyを使い、過去学習の移行元をIndexedDBのraw牌画像だけに依存していた。ユーザー端末にはv45のlocalStorage feature library（学習済み11種類）が残っていたが、raw画像が取得できない場合のfallback移行がなかった。そのためv46 keyは空のまま初回学習扱いになった。
**修正:** v47ではraw画像があれば従来どおりv47射影featureを再生成し、raw画像が空の場合はv46→v45→v44のlocalStorage libraryを順に探索。16×24のgray/edge/red/greenマップを現在の`perspective-direct-v1`として変換し、v47 keyへコピーする。結果画面には`元:v45`等を表示して、どの学習元を使っているか確認できるようにした。
**注意:** legacy featureは過去の正規化方式で作られたため、v47の新規射影featureと完全同一ではない。目的は学習0へ戻すことを避け、ユーザーの再ラベル負担を減らしつつ、新しいv47教師画像で徐々に置き換えること。
**再発防止:** feature形式や保存keyを更新するときは、raw画像の存在を前提にしない。旧feature libraryしか残っていない端末からも最低限の移行経路を持つ。版上げ後の実機テストには「旧版学習済み→新版起動で学習件数が0にならない」を必須項目にする。
**回帰テスト:** Chromiumでv45形式のlegacy libraryをlocalStorageへ作り、v47の`loadLegacyLibrary()`が同じラベルを`perspective-direct-v1`へ変換できることを確認。既存の射影補正・14枚撮影・学習保存・連続候補UI・通常対局/採点フローも全成功。
**確度:** v46で旧localStorageを読まないことはコード上確定。実機で2回連続初回学習になった症状も確定。raw IndexedDBが空だった理由自体は端末内DBを直接検査できないため未確定。

## M067: v47は射影14/14でもTop1 8/14で、旧rankingは学習枚数の多い牌種を有利にし得た
**時期:** M7 v47→v48
**症状:** iPhone実機でv47は学習済み11種類、元:raw、射影14/14、高信頼0枚、Top1 8/14。v45の並び替え8/14と同率で、射影補正を14枚すべてに適用しても精度は上がらなかった。またTop1に同じ牌種（例：6筒）が繰り返し現れる偏りが見えた。
**確定している設計上の偏り:** v47の`rankLabelsRobust`は、各牌種のテンプレート距離を全件計算して小さい方から最大3件を平均し、さらにサンプル1件の牌種だけpenaltyを付ける。学習枚数が多い牌種ほど「たまたま近いサンプル」を複数持てる機会が増えるため、候補数の違いがrankingへ影響し得る。6筒偏りの全原因がこれだとはまだ断定しない。
**修正:** v48では各牌種の全テンプレートを同じ重みで平均した**クラスprototypeを1個だけ**作り、query対prototypeを1回だけ比較する`rankLabelsBalanced`へ変更。牌種ごとの学習枚数は順位決定の比較回数に影響しない。
**解像度改善:** v44〜v47の直接比較は16×24（384px）だった。v48では24×36（864px）へ増やし、筒子の点配置・萬子の細線など小さい形状差を残す。保存済みraw画像から新版featureを自動再生成し、rawがない場合は旧16×24 featureを24×36へbilinear補間してfallbackする。
**再発防止:** 学習サンプル数が牌種ごとに不均等な状態でnearest-template上位k平均を主rankingに使わない。classごとに同じ個数のrepresentativeで比較する。feature解像度を下げる場合は細部識別への影響を実牌で確認する。
**回帰テスト:** 「多数sampleクラスには偶然近い1枚がある」fixtureで旧robust rankingが多数sample側を選ぶ一方、v48 balanced prototype rankingがsample数に左右されず安定クラスを選ぶことをunit/Chromiumの両方で確認。24×36 descriptor、legacy 16×24→24×36変換、既存射影・学習・UI・採点回帰も成功。
**確度:** v47のTop1 8/14・射影14/14・高信頼0は実機で確認。旧rankingのsample-count非対称性はコード上確定。v48で実牌精度が上がるかはiPhone確認待ち。

## M068: balanced prototype＋高解像度化でもTop1は7/14で、誤りの多くが牌種ファミリーをまたいだ
**時期:** M7 v48
**症状:** iPhone実機で学習済み11種類、元:raw、均等比較、射影14/14、高信頼0枚、Top1 7/14。v47の8/14から改善せず1枚低下した。
**観察:** 誤り7枚のうち少なくとも5枚が索→筒、字牌→筒、萬→字牌などのファミリー跨ぎだった。現在のglobal rankingは、牌種間の細分類以前に大分類を誤るケースが多い。
**判断:** sample-count bias除去と24×36解像度増加だけでは本質改善にならなかった。次は全11種類を一度に競わせる単段rankingではなく、萬/筒/索/字牌を先に判定し、そのファミリー内だけで牌種をrankする二段階認識を検討する。
**再発防止:** 実機誤りを単に正解/不正解だけで集計せず、同ファミリー内誤認とファミリー跨ぎ誤認に分解する。ファミリー跨ぎが多数なら細分類距離式の微調整を先にしない。
**確度:** Top1 7/14・射影14/14・高信頼0は実機画像で確定。ファミリー跨ぎが多数という観察も画像から確認。二段階認識が改善するかは次版で検証が必要。

## M069: v48の誤りはファミリー跨ぎが多く、全牌種を一度にrankする単段分類では大分類ミスを抑えにくい
**時期:** M7 v48→v49
**症状:** v48実機はTop1 7/14、高信頼0。誤り7枚のうち少なくとも5枚が索→筒、字牌→筒、萬→字牌などのファミリー跨ぎだった。
**根本原因候補:** 現在のglobal rankingは全学習済み牌種を同時に競わせるため、細部が偶然近い別ファミリーのprototypeが1位になると、その後に『これは萬か筒か』という大分類を取り戻す段階がない。v48でsample-count biasと解像度を改善しても跨ぎ誤りが残ったため、比較構造自体が次の検証対象。
**修正:** v49では各牌種prototypeをまず萬/筒/索/字牌へ等重みでまとめ、queryに最も近いfamilyを1つ選ぶ。その後、そのfamilyに属する牌種だけを`rankLabelsBalanced`してTop1を出す二段階rankingへ変更。高信頼自動確定は牌種marginだけでなくfamily marginも満たす場合だけ許可する。
**保存互換:** feature形式はv48と同じ24×36 perspective-direct-v1のため、storage keyはv48のまま維持して11種類の学習をそのまま使用する。版上げだけで再学習させない。
**再発防止:** 実機誤りにファミリー跨ぎが多い場合、解像度や閾値だけをいじる前に分類階層を見直す。新版でfeature形式が変わらないなら不要なstorage key変更をしない。
**回帰テスト:** global balanced rankingでは単独の別ファミリーoutlierが勝つfixtureを用意し、family-first rankingでは正しいfamilyを選び、そのfamily外のlabelが最終候補に入らないことをunit/Chromiumで確認する。
**確度:** v48の跨ぎ誤りは実機画像で確認済み。単段rankingの構造はコード上確定。二段階化が実牌Top1を改善するかはv49実機確認待ち。

## M070: v50はファミリー一致11/14でもTop1 6/14で、同family内の細分類が主ボトルネックになった
**時期:** M7 v50→v51
**症状:** iPhone実機で学習済み11種類、ファミリー補助、射影14/14、高信頼0、Top1 6/14。最終Top1のfamilyだけなら11/14合っていたが、同family内で7索→6索、5筒→6筒、筒子同士の誤認が多かった。
**判断:** v48ではfamily跨ぎ誤りが目立ったが、v50ではsoft family priorにより大分類は改善した一方、family内の数字/模様の識別が残った。family priorをさらに強めても同family誤認は直らない。
**修正:** v51では各family内の牌種prototype間の画素分散からdiscriminative weight mapを作り、牌種間で差が大きい領域を0.35〜3.0倍で強調して直接画像距離を計算。萬子の共通『萬』など全牌に共通する領域の影響を相対的に下げる。従来global距離も16%残し、過適合を抑える。
**再発防止:** 認識誤りをfamily跨ぎとfamily内誤りに分けて集計する。family一致率が高いのにTop1が低い場合、family priorやhard gateをいじらず、family内識別特徴を改善する。
**回帰テスト:** synthetic萬子fixtureで牌種差のある上段画素の重みが共通領域の2倍超になることと、差分強調rankingが正しいラベルを1位にすることをunit/Chromiumで確認。
**確度:** v50 Top1 6/14・高信頼0・画像上のfamily一致11/14は実機スクリーンショットから確認。v51が実牌精度を改善するかはiPhone確認待ち。

## M071: crop品質と認識器の限界を分離せずにranking方式だけを続けて変更していた
**時期:** M7 v51→v52
**症状:** 射影補正14/14まで達してもTop1は7〜8/14程度で、同じ認識器の重み・階層化・差分強調を変更しても改善が安定しなかった。一方で「現在のcropそのものが隣牌・外周・背景をどれだけ含んで精度を落としているか」を同一写真で比較する対照実験がなかった。
**根本原因:** 検出/crop/正規化と分類/rankingを一つの総合精度だけで評価していたため、どちらが主ボトルネックか切り分けできないまま認識器側の変更を続けた。
**修正:** v52を診断版とし、同一の正規化牌画像から (A)従来の通常crop と (B)外周を左右12%・上下8%除去して再拡大した内側crop を作る。保存済みraw教師画像にも同じ内側cropを適用して別ライブラリを構築し、**同じv51認識器**で両方のTop1を同時表示する。内側cropは学習データへ保存せず、純粋な比較に限定する。
**判定:** 内側cropのTop1が通常cropより大幅に上がればcrop/外周/背景側を優先改善する。ほぼ変わらなければ現在の画像比較recognizer/dataset側を主原因候補とし、CNN分類器（MobileNet系など）の検証へ進む。
**再発防止:** 認識精度が停滞した場合、分類器を変更する前に入力品質のA/Bテストを同一写真・同一教師データで行う。検出/正規化と分類器の評価を分ける。
**回帰テスト:** Chromiumでraw教師画像から内側crop診断ライブラリを再構築できること、通常Top1候補UIと内側Top1候補UIを同時に生成できること、通常の学習保存が従来featureのままで診断featureを保存しないことを確認する。
**確度:** 原因の未分離は開発手順上確定。cropが主因かclassifierが主因かはv52実機比較で判定する。

## M072: v52診断版で内側cropは大幅改善したが、raw診断ライブラリの二重構築で結果表示が約15秒まで遅くなった
**時期:** M7 v52→v53
**症状:** iPhone実機で同一14枚に対し、通常crop Top1は9/14、内側crop Top1は12/14まで改善した。一方、撮影から結果表示まで従来より明確に遅く、ユーザー実測で約15秒かかった。
**原因:** 精度面では外周・隣牌・背景を含む通常cropが主ボトルネックだったことがA/Bで確認できた。速度面ではv52が通常libraryに加えてIndexedDBのraw教師画像を全件順次decodeして診断用inner libraryを別途構築し、撮影時にその完了も待っていた。raw保存は1ラベル最大10枚なのに、本番rankingで使う上限5枚を超える画像まで処理していた。
**修正:** v53では内側cropを本番featureへ昇格し、二重診断rankingを削除。新しいv53専用library keyを使い、raw教師画像からの初回移行は各ラベル最新5枚だけを選び、画像decode/feature生成を`Promise.all`で並列化して一度だけlocalStorageへ保存する。その後の撮影では保存済みv53 libraryを直接使う。
**再発防止:** 精度診断用の重いA/B処理は恒久本番経路に残さない。raw画像再構築では最終的に利用するテンプレート数以上を無駄にdecodeしない。実機では精度と同時に「撮影→結果表示」の体感時間も記録する。
**回帰テスト:** v53で診断用second library/second rankingがコードから消えていること、inner cropが本番featureとraw再構築の両方へ適用されること、raw移行が各ラベル上限5枚かつ並列処理であることを確認する。
**確度:** 通常9/14・内側12/14・約15秒は実機スクリーンショットとユーザー報告で確定。15秒の全てがraw診断library構築由来かは実機プロファイラ未実施のため未確定だが、v52コード上で重複処理が存在したことは確定。

## M073: v53でv52の実際のlibrary keyを移行対象から漏らし、再び初回学習へ戻った
**時期:** M7 v53→v54
**症状:** v52では学習済み11種類を使えていたのに、v53ではiPhone実機の1回目・2回目とも「初回学習：14枚を指定してください」となった。結果表示自体はこれまでで最速だった。
**根本原因:** v52/v51の`LIB_KEY`は`MahjongScoreApp_tile_templates_m7v48balanced24x36`を継続利用していた。一方v53の`LEGACY_LIB_KEYS`はv47/v46/v45/v44しか列挙せず、実際に端末へ残っているv48 keyを探索しなかった。raw IndexedDB再構築が使えない端末ではfallbackも0件になった。
**修正:** v54ではv48 keyを明示追加し、さらに`MahjongScoreApp_tile_templates_` prefixのlocalStorage keyを自動探索して将来版も拾う。通常crop時代のfeatureは左右12%・上下8%相当をfeature-map上でcrop-resampleしてinner-crop形式へ近似変換する。v53 inner-crop featureは二重cropしない。
**速度上の注意:** v53が最速だったという実機報告は有益だが、その2回はlibrary 0件で本番rankingを実行していないため、認識込みの速度改善としては未確定。v54で学習libraryを復旧した状態の速度を再測定する。
**再発防止:** 新版で保存keyを変更するときは、直前版の表示version名ではなく**実際のLIB_KEY文字列**を履歴から確認する。可能な限り保存schemaをversion表示と分離し、互換schemaならkeyを変えない。legacy migrationはprefix探索の回帰テストを持つ。
**回帰テスト:** v48 keyだけがlocalStorageに存在する状態をChromiumで作り、v54の`loadLegacyLibrary()`がラベルを復旧し24×36の`perspective-direct-v1`へ変換できることを確認。
**確度:** v53のlegacy key漏れはコード上確定。v53の速度が最速だったことはユーザー実機報告で確定。ただし認識込み速度は未確定。

## M074: near-top-down撮影なのに強い射影warpを許し、牌画像を逆に斜め・台形へ変形させた
**時期:** M7 v46〜v54→v55
**症状:** iPhone実機の結果画面で、元の手牌より一部牌画像が明らかに斜め・台形に変形して表示された。射影14/14という表示自体は成功数であり、補正の品質保証にはなっていなかった。
**根本原因:** `detectFaceQuad()`の受理条件が面積・大まかな縦横比中心で、上/下辺や左/右辺の長さ差、対向辺角度、四隅の直交性を十分に制限していなかった。牌の内部模様や背景境界を四隅として拾っても`warpPerspective`相当処理を強制する場合があった。
**修正:** v55ではnear-top-downという製品側制約を利用し、強い台形・平行性の崩れ・角度異常のquadを拒否。拒否時は回転補正のみへfallbackする。結果画面も実recognition inner cropを表示して、補正品質を直接確認できるようにした。
**再発防止:** `射影採用 X/14`を成功指標として扱わない。補正後画像の幾何品質とTop1精度をセットで確認する。製品の撮影条件が真上寄りなら、理論上可能な強いperspectiveまで無理に補正しない。
**回帰テスト:** source testで長辺比・対向辺角度・corner orthogonality guardが存在すること、結果表示cropがrecognition入力と同一であることを確認。
**確度:** 実機で斜め変形が発生したことは画像で確定。誤ったquad受理が主因という説明はコード構造から有力。

## M075: versionごとに学習保存keyを変え続け、実機で毎回初回学習へ戻る状態を作った
**時期:** M7 v44〜v55→v56
**症状:** v52では学習済み11種類が見えていたが、v53以降はmigration修正を重ねてもiPhone実機では初回学習表示が継続した。
**根本原因:** 認識feature形式の変更ごとにlocalStorage keyをversion付きで変更し、旧key/raw IndexedDBからのmigration成功を前提にした。端末の実際の保存領域・実行コンテキストで旧データが見えない場合、migrationコードを増やしても0件のままになる。
**修正:** v56から通常保存先をversion非依存の`MahjongScoreApp_tile_templates_stable1`へ固定し、backup keyにも二重保存する。今後feature schemaを変える必要がある場合はstable schema自体を明示的にversion-upし、通常のUI build番号ではkeyを変えない。
**運用判断:** 旧データ救出はここで打ち切り、iPhone上で一度だけ14枚を正しく確定してstable libraryを作る。その後、次回撮影で学習済み表示が維持されることを最優先で確認する。
**回帰テスト:** Chromiumでstable libraryを保存し、primaryを削除してもbackupから同じラベルが復旧することを確認。
**確度:** 実機で初回学習が継続したことは確定。旧保存データが現在の実行コンテキストから見えない理由自体は端末storageを直接検査できないため未確定。

## M076: 初回学習の連続入力で撮影した牌を表示せず、ユーザーに記憶させるUIになっていた
**時期:** M7 v39〜v56→v57
**症状:** 14枚手動指定画面には牌候補一覧と1〜14の入力位置だけがあり、現在入力対象の実際の撮影牌が見えなかった。ユーザーが「ぱっと見どれだったっけ」と元画面を記憶しながら入力する必要があった。
**根本原因:** 認識結果画面のcropは`state.pendingCrops`に保持していたが、tile pickerは牌名入力専用UIとして作られ、cropを参照していなかった。
**修正:** v57で現在indexの`state.pendingCrops[index]`をtile pickerへ表示し、連続入力のindex変更イベントに追従させる。候補Top3がない初回学習でも表示する。
**再発防止:** 手動訂正UIでは、訂正対象の元入力を同一画面に必ず残す。番号だけで元対象を思い出させない。
**回帰テスト:** Chromiumで1枚目pickerを開いた時にdata URL画像が表示され、通常グリッドから1枚選んで2枚目へ進んだ後にpreview srcが変わることを確認。
**確度:** UX問題は実機スクリーンショットとユーザー報告で確定。

## M077: 学習保存をfire-and-forgetし、実機で保存失敗しても次画面へ進めていた
**時期:** M7 v56〜v57→v58
**症状:** ユーザーはv56で14枚を手動指定して「この手牌で進む」まで実施したが、確認用のv57再撮影でも初回学習のままだった。
**根本原因:** M7保存処理はdocument capture listenerで発火するだけで、ui-fixesの結果遷移は保存結果を待たずにrootをremoveして次へ進んでいた。`saveLibrary()`も例外をcatchして黙って0を返す設計だったため、実機保存が失敗してもユーザーには成功したように見えた。テストも最終localStorage値だけを見ており、実UI遷移が保存成功を待つことを保証していなかった。
**修正:** v58で`persistVerifiedHand(root)`をidempotentなPromise APIとして実装。14ラベル/featureを検証→primary+backup保存→read-back検証→raw IndexedDB保存の順で処理し、ui-fixes側がawaitする。失敗時は遷移を止めて入力済み14枚を残す。
**再発防止:** 重要な保存を行うボタンは、保存完了・read-back検証前に画面を閉じない。保存APIは成功/失敗を戻し、UI側がその結果で遷移を決める。catchして無言で続行しない。
**回帰テスト:** source testでui-fixesが`await window.M7CameraV36.persistVerifiedHand(root)`を呼ぶことを確認。browser smokeで実際の「この手牌で進む」クリック後にstable keyへ14ラベルが保存されることを確認。
**確度:** v57再撮影でも初回学習だったことはユーザー実機で確定。旧UIが保存完了を待たず遷移する構造はコード上確定。


## M078: v58の保存失敗表示は原因を潰せず、localStorageを学習の単一障害点にしていた
**時期:** M7 v58→v59  
**症状:** iPhone実機で14枚を確定して「この手牌で進む」を押すと、v58が意図どおり遷移を止めた一方、「学習データの保存に失敗しました」だけが表示され、どの保存段階で失敗したか判別できなかった。  
**確定している設計上の原因:** v58の`saveLibrary()`はprimary / backup / metaを1つのtry内でlocalStorageへ連続保存し、どれか1つの失敗をすべて同じ0へ潰していた。また学習の利用経路もlocalStorage読込を前提にしており、すでにIndexedDBへ保存しているraw教師画像を永続fallbackとして使い切れていなかった。iPhone実機で実際にどのsetItemが失敗したか自体は未確定。  
**修正:** v59ではraw教師画像を先にIndexedDBへ保存・read-back確認し、これを耐久fallbackに昇格。localStorageは高速cache扱いへ変更し、primaryだけを直接read-back検証する。backup/meta失敗だけでは保存全体を失敗にしない。primaryが容量等で失敗した場合は旧version付きtemplate keyを整理し、rawが安全に保存済みなら古いbackupも退避対象にしてprimaryを再試行する。localStorageが最後まで失敗しても、IndexedDBが検証済みならruntime libraryを使って進み、次回起動時にrawから再構築する。失敗時はreason / storage stage / cleanup数を画面へ表示する。  
**再発防止:** 大きい学習データをlocalStorageだけに依存させない。primary / backup / metadataを別々に検証し、補助backupの失敗をprimary成功と同一扱いにしない。実機エラーはgeneric文言だけでなく原因コードを残す。  
**回帰テスト:** Chromiumで通常保存、backupだけQuotaExceededErrorになるfixtureでもprimary成功なら全体成功になること、stable localStorageを削除後にIndexedDB rawから14ラベルを再構築できることを確認する。  
**確度:** v58で保存失敗画面に止まったことは実機で確定。v58のエラー集約とlocalStorage単一障害点はコード上確定。実機で最初に失敗した具体的APIは未確定で、v59の詳細コードで次回失敗時に確定する。


## M079: 連続入力pickerへプレビューと候補を足した結果、iPhone横画面の縦予算を超えた
**時期:** M7 v57〜v59→v60  
**症状:** iPhone横画面の「手牌を連続入力」で、1〜14選択・撮影牌プレビュー・Top3候補までは見えるが、その下の全牌一覧と「閉じる」が画面外へ押し出され、スクロールしないと操作できなかった。  
**根本原因:** 元のpickerは牌一覧中心の高さ設計で、後から撮影牌プレビューとTop3候補を縦方向へ追加したのに、14スロット40px・余白・9列牌グリッド・94vhカードの縦予算を再計算していなかった。牌グリッドの`overflow:auto`が問題を隠し、「一画面完結」という製品要件を満たさなくなった。  
**修正:** v60ではiPhone横画面向けに`100dvh`とsafe-areaでカードの実表示高を固定し、タイトル/14スロット/ヘルプ/撮影画像/Top3/閉じるをコンパクト化。全34牌を12列×3行へ配置し、牌一覧の内部スクロールを禁止して同一画面へ収める。連続入力・任意スロット戻り・プレビュー/候補追従のJS挙動は維持する。  
**再発防止:** モーダルへ補助UIを追加するたびに、各固定ブロックの高さと残り可変領域をiPhone PWA相当の実viewportで再計算する。スクロール可能になったことを「収まった」とみなさない。  
**回帰テスト:** 932×430のモバイル横画面Chromiumで、カード・閉じるボタンがviewport内、牌34個が全て存在、牌グリッドのscrollHeight<=clientHeight、overflow-y=hiddenを確認する。  
**確度:** iPhone実機で下部見切れはスクリーンショットで確定。原因となる旧CSSの縦積み構造もコード上確定。

## M080: 高信頼0枚のまま固定閾値だけでは、同family内の学習ばらつきと競合差を評価できなかった
**時期:** M7 v59→v60  
**症状:** v59実機で学習済み11種類を読めてTop1候補は14枚出る一方、高信頼は2回とも0枚。同一手牌でも射影採用数が12/14→9/14と変動した。  
**根本原因候補:** 高信頼判定は主にTop1絶対距離・Top1/Top2固定gap/ratioを見ており、その牌種の教師テンプレート自体がどれだけ散らばっているか、最も近い同family競合との分離が学習ばらつきに対して十分かを使っていなかった。また射影採用は四辺形の最終形状しきい値が中心で、辺fitの残差品質や「そもそも射影補正が必要な歪み量か」を明示評価していなかった。  
**修正:** v60では各ラベルと同family競合prototypeの差が大きい画素を強調するlabel-specific weight mapを追加し、family-wide mapと合成。各ラベルのtemplate spread、同family runner-up gap/ratioもranking診断へ追加し、高信頼はclass compactnessに応じた必要marginを満たす場合だけ許可する。射影は四辺fit RMSEが悪い候補を拒否し、歪みがごく小さい場合は無理にwarpせず回転補正へ戻す。  
**保存互換:** `perspective-direct-v1`の24×36 feature形式と`MahjongScoreApp_tile_templates_stable1`は変更しないため、v59のstable学習データをそのまま使用する。  
**再発防止:** 高信頼数を増やす目的で固定閾値だけを緩めない。Top1/Top2距離に加えて、同class spreadと同family competitor separationを記録して判断する。射影採用数そのものを品質指標にせず、fit品質と補正必要量を確認する。  
**回帰テスト:** synthetic同family fixtureでlabel-specific mapが識別画素を強調し正しい牌を1位にすること、template spreadを計測できること、source testでfitResidual/perspectiveNeed guardが存在することを確認する。実牌でのTop1・高信頼・射影安定性改善はiPhone実機で次に確認する。  
**確度:** 高信頼0枚と射影12/14→9/14はユーザー実機報告で確定。固定confidence構造とfit残差未使用はコード上確定。v60の実牌精度改善量は未確定。


## M081: v60は同じ手牌でTop1 8/14→10/14だが、crop位置が変動して隣牌が混入した
**時期:** M7 v60→v61  
**症状:** 同じ14枚のiPhone実機2連続撮影でTop1は1回目8/14、2回目10/14。学習済み11種類・元stableは維持できた一方、高信頼は両方0枚、射影採用は5/14→2/14。特に同じ西・萬子・索子でも撮影ごとにTop1が変わり、結果画面のcropには隣牌の模様や木目が入り込む箇所が見えた。  
**根本原因:** 現在の本番経路は`analyzeGuideCanvas()`で牌列全体の外接rowを求めた後、その`x/w`を単純に14等分している。row外接の左右端が撮影ごとに少し変わると、14個すべての境界位相も一緒にずれ、隣牌の模様が混ざる。過去v37の`splitRowBySeams`は各境界を独立に動かして牌模様をシームと誤認したため再利用できない。  
**修正:** v61では個別シームを復活させず、14枚全体に共通する「1つの開始位置＋1つの牌ピッチ」だけを周期的な縦境界証拠から最適化する。13個の内部境界の平均・中央値・下位支持率をまとめて評価し、十分な周期証拠と改善量がある時だけrowのx/wを補正。証拠が弱い場合は従来14等分へ安全にfallbackする。高信頼0枚の切り分け用に、距離・同family差・family競合など保留理由の集計も結果画面へ出す。  
**保存互換:** stable保存key、24×36 `perspective-direct-v1` feature schema、raw IndexedDBを変更しない。v59/v60で保存済みの11種類をそのまま利用する。  
**再発防止:** crop境界改善では各境界を独立に模様へ追従させない。全14枚で共有する低自由度の幾何モデルを優先し、同じ手牌を連続撮影した時のcrop位置とTop1安定性を評価する。  
**回帰テスト:** 合成14牌列で、意図的にずらしたrow外接からglobal phase/pitch fitが真の開始位置・牌幅へ近づくことをChromiumで確認。弱い周期証拠ではfallbackすること、保存互換・iPhone横picker・通常対局/採点フローも既存テストで確認する。  
**確度:** Top1 8/14→10/14・射影5/14→2/14・高信頼0は実機スクリーンショットから確定。14等分が本番経路であることはコード上確定。crop混入への寄与はスクリーンショットと構造から高確度。


## M082: v61の全体格子補正は実機で4/14まで悪化し、未補正の同手牌は10/14だった
**時期:** M7 v61→v62  
**症状:** 同じ手牌の連続実機テストで、1回目は「全体格子補正」が発動しTop1正解4/14・射影4/14・高信頼0。2回目は全体格子補正が発動せずTop1正解10/14・射影4/14・高信頼0だった。1回目のcropは隣牌や端部混入が目立ち、補正が改善ではなく回帰になった。  
**確定した追加情報:** 高信頼0の保留理由は、1回目が「距離12・family競合2」、2回目が「距離14」。したがって高信頼閾値のmarginより前に、絶対距離側が主なボトルネック。  
**根本原因:** v61の周期fitは全14境界を1つのphase/pitchで動かすためv37より低自由度だが、牌模様の反復にも周期性があり、誤った位相へfitしたとき全14cropをまとめてずらしてしまう。  
**修正:** v62ではfit計算自体は診断用に残すが、本番cropへは適用しない。実機で相対的に良かった牌列rowの14等分へ戻す。高信頼の「距離」保留を「原型距離」と「実例距離」に分離し、class prototype側か最も近い学習テンプレート側かを次の実機テストで切り分ける。閾値は変更しない。  
**再発防止:** 幾何補正を本番採用する前に、同一手牌で補正あり/なしのA/Bを最低2回比較し、Top1とcrop目視の両方で改善を確認する。補正が発動したという事実自体を品質向上とみなさない。  
**回帰テスト:** `fitGlobalRowGrid` helperは単体で維持するが、`analyzeGuideCanvas`が`lowRow`をそのまま本番14等分へ使い`gridUsed=false`であることを確認。confidence diagnosticはprototype/template距離を別reasonで返す。  
**確度:** 4/14対10/14、補正発動有無、高信頼0と保留理由はユーザー実機スクリーンショットで確定。


## M083: v62は高信頼0枚の保留理由が14枚すべて原型距離だった
**時期:** M7 v62→v63  
**症状:** v62実機で学習済み11種類・元stable・高信頼0枚が継続し、保留理由は「原型距離14」。つまりTop1/Top2のmarginへ到達する前に、各クラスを画素平均したprototypeとの絶対距離で全14枚が止まっていた。  
**根本原因候補:** 同一牌でもcrop位置・角度・照明が少しずれるため、複数教師画像を画素ごとに単純平均すると線や筒子の輪郭がぼけた「実在しない平均画像」になる。その平均画像を代表として距離判定すると、最寄りの実教師画像には近いqueryでもprototype距離だけが大きくなり得る。  
**修正:** v63では保存済み教師画像から、クラス内の他教師との平均距離が最小の**medoid（実在する1枚）**を代表画像として使う。さらに最寄り1枚だけの偶然一致を避けるため、近い2〜3枚の距離を固定重みでまとめたtemplate consensusをrankingへ38%混ぜる。高信頼も、代表画像が少し遠い場合は「複数教師が近い」時だけ救済し、1枚だけ近いケースでは救済しない。固定閾値を単純に緩める変更ではない。  
**保存互換:** stable key、raw IndexedDB、24×36 `perspective-direct-v1` schemaは変更しない。v62までの学習済み11種類をそのまま利用する。  
**再発防止:** 複数画像の画素平均を画像分類の唯一のclass代表にしない。位置ずれが残る直接画像比較では、実在sampleのmedoid・複数sample合意・平均prototypeを別々の証拠として扱う。  
**回帰テスト:** unitでmedoidが算術平均ではなく実テンプレートを返すこと、outlierを含む3教師でも近い2教師のconsensusが優先されること、browserで代表距離が遠い1sampleは拒否・複数実例が近い場合のみconfidence通過することを確認。既存のv62牌列14等分・picker配置・保存復旧も維持する。  
**確度:** 「原型距離14」はv62実機画面で確定。画素平均によるぼけはコード構造上確定。v63のTop1改善量は実機確認待ち。


## M084: v63のmedoid＋実例合意でも代表距離が主因のまま、Top1が不安定だった
**時期:** M7 v63→v64  
**症状:** 同じ14枚を連続実機撮影したv63で、高信頼は両方0。1回目は保留理由「代表距離12・family競合2」、2回目は「代表距離14」。手動確定した正解手牌とTop1を照合すると、1回目は大きく崩れ、2回目も半数程度に留まり、同じ牌でも撮影ごとにTop1が変わった。  
**根本原因候補:** 算術平均prototypeをmedoidへ置き換えても代表距離が全14枚を止めたため、問題は「平均画像がぼけること」だけではない。24×36の直接画素比較が、残る数pxの位置ずれ・牌面の見かけサイズ差・照明差にまだ敏感である可能性が高い。現在の±1px/±2度/±3%変換だけでは、実機差を十分吸収できていない。  
**修正:** v64では保存済み24×36 featureから、6×9と3×5の多段階pooling、縦横投影を使う**形状距離**を追加。細い線が1pxずれても同じ大まかな配置として比較し、medoid＋複数実例の直接距離と60:40で合成する。高信頼は固定閾値を単純緩和せず、複数教師の形状合意が強い場合だけ画素代表距離の救済を許す。  
**保存互換:** stable key、raw IndexedDB、24×36 `perspective-direct-v1` schemaは変更しない。既存11種類の学習データをそのまま使って新しい形状距離を計算する。  
**再発防止:** 直接画像距離だけで精度改善を繰り返さず、「同じ形の小位置ずれ」と「異なる形」の距離を独立テストする。新しいdescriptorへ保存schemaを増やす前に、既存featureから派生できるロバスト特徴を優先してA/Bする。  
**回帰テスト:** 24×36 syntheticで縦線を1pxずらした同形状のstructural distanceが横線の別形状より小さいこと、複数同形状sampleのstructural consensusがoutlierより近いことをunit/Chromiumで確認。v62の牌列14等分、picker配置、保存復旧は維持する。  
**確度:** 高信頼0と代表距離12/14・14/14は実機画面で確定。直接画素距離の位置ずれ感度はコード上確定。v64の実牌改善量は実機確認待ち。


## M085: v64で高信頼は2→4枚へ増えたが、同じ手牌のTop1がまだ揺れた
**時期:** M7 v64→v65  
**症状:** 同じ14枚を連続撮影したv64で、高信頼は1回目2枚・2回目4枚まで増えた。一方で保留理由は代表距離12→10が中心で、8筒→5筒、西→1萬/5萬、3萬→2萬など同じ牌でもTop1が撮影ごとに変動した。高信頼になった牌は目視上正解しており、形状比較自体には改善が見られた。  
**根本原因候補:** v64は1枚につき1つの正規化cropだけを分類している。実機では同じ撮影条件でも牌面の左右位置・見かけサイズ・内側crop境界が数pxずれるため、形状距離を追加しても1つのcropが偶然悪い位置に寄るとTop1が変わる。  
**修正:** v65では保存用featureは従来のまま1個を維持し、推論時だけ同じcanonical画像から「標準・少し広い・少し狭い・左寄せ・右寄せ」の5つの近接cropを作る。各cropを独立にランキングし、ラベルごとの距離中央値＋小さなばらつきペナルティで統合する。高信頼は5視点のうち過半数が同じTop1を支持しない場合は自動確定しない。  
**保存互換:** stable key、raw IndexedDB、24×36 `perspective-direct-v1` schema、保存時の代表featureは変更しない。既存学習済み11種類をそのまま利用する。  
**再発防止:** 実機の数px揺れを1つのcropへ押し込めず、保存schemaを変える前に推論時の複数view合意で吸収する。高信頼数だけでなく、同じ手牌2連続のTop1一致率も見る。  
**回帰テスト:** coreで5つのview中4つが同じラベルを支持する時に1つの外れviewを無視できること、iPhone相当Chromiumで5個のfeature viewが24×36の既存schemaを維持すること、過半数未満のview一致ではconfidenceが`view-disagreement`で止まることを確認する。  
**確度:** 高信頼2→4、代表距離12→10、Top1変動は実機スクリーンショットで確定。数pxのcrop揺れが主因かは有力だが、v65の実機改善量は未確定。

# 変更前に特に見る高頻度項目
1. **古いパッチとの競合**（M001/M003/M004/M006/M007）
2. **実DOMとCSS前提**（M011/M015/M017）
3. **iPhone実表示領域**（M012/M013）
4. **MutationObserver自己再発火**（M023/M024）
5. **カメラtrack停止**（M022）
6. **14枚/和了牌/門前/ロンツモ/面子状態のデータ源**（M027-M032）
7. **保存・復旧に新状態を含めたか**（M035/M036）
8. **index.html読込・badge・Pages SHA**（M005-M008）
9. **点数表のscroll/footer**（M014-M017）
10. **開発時間はdevelopment-time.mdのみ**（M037）

---

# 今後の追記フォーマット
```
## M0XX: 短いタイトル
**時期:**  
**症状:**  
**根本原因:**  
**修正:**  
**再発防止:**  
**回帰テスト:**  
**確度:** 確定 / 有力 / 未確定
```
