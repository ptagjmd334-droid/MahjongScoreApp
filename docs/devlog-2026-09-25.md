# MahjongScoreApp 開発記録 2026-09-25

## 作業時間
- ユーザー申告：1時間00分（9月24日夜〜25日未明の会話・作戦整理）。
- 更新後の累計：26時間00分。正本は `docs/development-time.md`。

## 本日の作業・確認
- 旧チャットの引継ぎ情報を確認。GitHub main の引継ぎ時点の最新コミットは `9bc196d3c3cc54bf7b5686ad130a155e758ab62d`、アプリ表示は M7 v35。
- ユーザーが iPhone で実牌14枚を机の上に密着させて横一列に並べて撮影。カメラ画面は候補 `0/14` のまま。確認ボタンで確認画面へ進めたが、牌列が抽出されず撮影画像全体＋14個の未確定欄が表示された。自動牌種認識も実行可能な切り出し画像がなく、一枚も認識されていない。
- この結果は、v35 の「列が見つからなければ全画像を表示する」フォールバック自体は動いたことを示すが、牌列検出・14枚切り出し・牌種認識の完成は示さない。
- 今回は症状と開発方針のみ整理。実牌で検出が0になるコード上の直接原因は未確定。新たなコード実装・公開・自動テスト・iPhone合格判定は行っていない。

## 今回決定した仕様・開発方針
- 認識開発方針は **C：まず手動の補助操作も使って実用化し、その後に完全自動認識へ段階的に進める**。
- 撮影方法は **A：アガった手牌を机に寝かせ、14枚を横一列に並べて上からiPhoneで撮影する**。立てた牌への対応を今回の必須条件に追加しない。
- 目標は、牌列を自動発見→13〜14枚の切り出し・枚数認識→牌種自動判定→誤りだけ修正→点数計算。手動切り出しだけでM7全体を完了扱いにしない。
- 次版（仮称 M7 v36）案：既存自動検出を残し、失敗時は撮影画像に牌列全体の範囲指定・位置/サイズ調整を設け、その範囲だけを14分割して各牌の実写プレビューを表示する。正しい牌名を指定・確定できるようにする。14等分がずれる場合の境界補正は実画像で評価する。保存済みの実牌切り出し画像と正解ラベルを再利用して、認識方式の精度を検証する構想。
- 次版の具体的な修正内容と試験は、最新main・管理ファイル・既存カメラ処理を確認して確定する。バージョン番号も実装時に最新状態から決める。
- 競合アプリの実機UIを参考にし、M7の方針を追加更新することをユーザーが承認。**リアルタイムで14個の物体を完全検出してから進むことを必須にせず、固定の手牌撮影枠＋シャッター方式を中心にする。** 撮影後に枠内を解析し、牌列補正→14枚分離→牌種認識→誤認識だけ高速修正→和了牌指定→採点、という流れを目標とする。
- 競合アプリから参考にするのは機能設計・UXの考え方であり、コードや画像素材を流用しない。ドラ表示牌の別枠撮影、追加役の手動指定、符・役・基本点の説明表示は将来候補。現在のM7ではまず手牌14枚の読み取りを優先する。
- 次回M7では、実画像による0/14原因調査、高解像度撮影画像の保持、固定撮影枠、シャッター後解析、誤認識修正UIを優先的に検討する。

## 未完了・注意
- M7 v35 の実牌検出は不合格。候補0/14、実牌列の位置推定失敗、個別画像未取得。根本原因は未確定。
- 役・符・点数計算の次段階や副露・カン入力に勝手に進まない。
- iPhone実機での最終合格報告なし。作戦整理終了につき本日の開発はここまで。

## 次回の開始地点
1. GitHub main と `AGENTS.md`、`docs/user-development-rules.md`、`docs/pre-change-checklist.md`、`docs/known-mistakes-and-prevention.md`、M7 カメラ本体・認識コア・関連テストを確認。
2. 0/14 と牌列推定失敗の根本原因を、今回の実牌画像とコードで調査。「確定」「仮説」「実機ログが必要」を区別する。
3. 同じカメラ段階で **検出失敗への修正＋手動範囲指定・14枚切り出し・個別プレビュー** をまとめて実装する（仮称 M7 v36）。
4. 可能な自動テスト・GitHub Pages 公開状況を確認してから、一度のまとまった iPhone 実機チェックを提示。ユーザーのOKまで次の段階へ進まない。

## 追記：M7 v36 実装再開
- ユーザーの「続きやろか」を受け、決定済みの固定撮影枠＋シャッター方式で実装を再開。
- 実機0/14の原因調査で、旧ライブ検出が480×190の全画面縮小画像に対して個別連結成分を探し、牌候補の高さを `h*0.34` 未満なら除外していることを確認。実機スクリーンショットの牌スケールと不整合があり、密着牌を14個の個別物体として扱う設計も不利。詳細は M053 に記録。
- 新規 `m7-camera-v36.js` を追加し、公開入口を v36 に変更。ライブ中の13/14個検出を必須にせず、固定白枠へ合わせて「撮影して読み取る」を押す方式へ変更。
- シャッター時はCSSの `object-fit: cover` を考慮し、白枠に対応する元動画の高解像度領域を直接取得。低解像度コピーで横一列の牌帯を推定し、高解像度側を14分割してプレビュー・学習特徴へ使用する。
- 牌列推定に失敗した場合は白枠内の写真を表示して手動入力へ退避。偽の背景14分割は出さない。既存の「間違っている牌だけタップ修正」と学習フローは維持。
- 自動テストを v36 に更新し、固定枠の座標変換、密着牌列、14分割、高解像度プレビュー、シャッター/キャンセル非重複を確認対象に追加。
- この時点ではGitHub反映まで。GitHub Actions / Pages公開 / iPhone実牌最終確認は別途確認し、ユーザーのOKまではM7を合格扱いにしない。

## 追記：M7 v36 hardening・main反映
- 実牌スクリーンショットとv35コードを照合し、ライブ0/14の主要な設計不整合を確認。全画面480×190に対する個別牌高さ34%以上という条件は、実際の撮影構図の牌スケールと合っていなかった。密着牌を個別連結成分として14個探す方式も廃止方向とした。
- M7 v36をさらにhardening。固定撮影枠を実牌列向けに細くし、シャッターを中央配置。牌列探索は固定の明るい白閾値ではなく、輝度と色差の比から中性色を判定する方式へ変更した。暗めの実牌を模したブラウザテストへ更新。
- 白枠のCSS表示領域を `object-fit: cover` の元動画座標へ戻して高解像度で取得する設計を維持。位置探索は低解像度、牌の切り出し/特徴取得は高解像度という役割分担にした。
- アプリがbackground/非表示へ移った場合にもカメラの既存キャンセル経路を呼び、MediaStream停止漏れを防ぐ処理を追加。
- Service Workerのキャッシュ名をM7 v36へ更新し、`m7-recognition-core.js` と `m7-camera-v36.js` をプリキャッシュ対象へ追加。
- workブランチのGitHub Actions（run 39）は、役・符テスト、M7ユニット/読込順、Chromium横画面の固定枠シャッター→14分割→アガリ/点数表まで**すべて成功**。失敗していた旧runはlegacy `m8-v30.js` がbuild badgeをM7 v35へ戻す既知競合で、mainの修正を同期して解消した。
- hardeningはPR #1経由でmainへマージ。merge commit: `59269f67acecee8985bbbca3ecec05c09f098114`。ただし、iPhone実牌確認はまだ未実施のためM7 v36は未合格。
- 次はGitHub Pagesの公開SHAを確認してから、同じ14枚を一度だけ並べる一括iPhoneテストを実施する。

## 追記：iPhone v36 14分割成功・表示/学習修正
- ユーザーのiPhone実機スクリーンショットで、白枠内の牌列検出と14分割が成功したことを確認。v35の主要失敗だった「牌列を見つけられず全画像へフォールバック」は解消方向。
- 一方、各プレビューが細長い縦ストリップ表示になっていた。原因は14列の縦長ボタンへ実写cropを `background-size: cover` で表示していたため。crop自体ではなく表示側の問題と確認し、`contain`＋有限高さへ変更。
- `0/14枚を自動候補化` は、初回の端末内学習テンプレートがないことに加え、確定時の学習listenerがbubble phaseだったため、既存button onclickがoverlayを閉じた後にラベル取得できない経路を発見。capture phaseへ変更し、閉じる前に保存するよう修正。
- 初回学習時は「14枚を1回指定すると次回候補に使う」と画面に明示するよう変更。
- 回帰テストで、14cropが全体表示（contain）、14ラベル確定後に14テンプレートがlocalStorageへ保存されること、通常の横画面対局/採点フローが壊れていないことを確認。work branch CI run 44は全成功。
- PR #2をmainへマージ。merge commit: `049014c27c251b1ea8992899cdd3da43d531348d`。次のiPhone確認では、同じ14枚を使って初回14枚手動指定→確定→同じ手を再撮影し、自動候補が0から増えるかを確認する。

## 追記：3回目0/14・位置ずれ耐性を追加
- iPhone実機で初回14枚手動学習後、2回目は6/14を自動候補化。2回目も14枚を正しく確定したが、3回目は同じ手牌で0/14へ低下。14枚の切り出し自体は成功したまま。
- 同一ラベルにテンプレートを追加しても最小距離は悪化しない設計のため、学習データが増えたこと自体が6→0を起こしたわけではない。撮影3回目の切り出し位置/模様位置の小さな差に、8×12グレースケール特徴比較が敏感なことを優先原因として修正。
- `m7-recognition-core.js` に±1セルの平行移動を許容するshift-tolerant RMS比較を追加。既存テンプレート形式・判定閾値・marginは維持し、過剰な誤認識を避ける。既存の端末学習データはそのまま利用可能。
- 上位3候補と距離を `M7V36LastDiagnostics` に保持するようにし、次回まだ不安定な場合は閾値を推測で変更せず距離情報から判断できるようにした。
- 1セルずれで通常RMSは大きくなるがshift-tolerant比較では正解ラベルが選ばれる単体テストを追加。work branch CIは全成功。
- PR #3をmainへマージ。merge commit: `1668c8a0cc0efe27b95b8ef6b4aae99f4dd09033`。次のiPhone確認では追加の手動学習は要求せず、保存済みテンプレートのまま同じ14枚を1回撮影して自動候補数を確認する。

## 追記：3回目0/14→牌面正規化
- iPhone実機の3回目撮影で、2回学習後にもかかわらず自動候補が6/14→0/14へ低下。学習保存自体は前回6/14で動作しているため、単純な未保存ではなく、撮影ごとの位置・背景差に現在の特徴量が弱いと判断。
- 旧featureは14等分した帯全体（木目や余白を含む）を32×48へ縮小して比較していた。実機画像では牌面が帯の一部しか占めず、わずかな上下位置・明るさ差で特徴量が大きく変わり得る構造だった。
- 各14分割内で明るく低彩度の牌面矩形を再検出し、その牌面だけを正規化して特徴量・プレビューに使用するよう変更。背景/余白の影響を減らした。正規化後のみ判定閾値を少し緩和。
- Chromium回帰テストに「背景付き分割から牌面だけを切り出せること」を追加し、既存の14分割・初回学習保存・対局/採点フローと合わせてCI全成功。
- PR #4をmainへマージ。merge commit: `5dcb817006d516f7a36440f61e786896561b5b69`。次の実機確認では、既存学習データは旧特徴量形式なので再利用せず、1回だけ初回学習をやり直してから同じ14枚で2回目認識率を測る。

## 追記：1/14→牌間シーム補正
- 牌面正規化後の初回学習→同じ14枚2回目撮影で自動候補1/14。これにより、単純な判定閾値ではなくcrop再現性の問題が残っていると判断。
- 2回のiPhone確認画面を比較すると、同じ物理牌が14等分枠の中で左右へずれ、境界が牌面へ食い込む位置も変化。右端の西だけ安定しやすい挙動とも一致。
- 14等分を廃止せず初期値として残しつつ、各内部境界の近傍で縦エッジ・白牌面占有率を評価し、13本の境界を実際の牌間シームへ寄せる補正を追加。
- 不均一な牌幅・隙間を持つ合成列で、境界誤差16px未満を確認する回帰テストを追加。既存の牌面正規化・初回学習保存・通常対局/採点フローを含めCI全成功。
- シーム補正後は入力特徴が変わるため学習ライブラリも新キーへ切替。PR #6（シーム補正）とPR #7（学習キー更新）をmainへマージ。最新merge commit: `a5ab0ac6638db7dd6eabe19ad10dfaf3ffe7042e`。
- 次の実機確認は再度1回だけ初回学習→同じ14枚を再撮影し、2回目の自動候補数と各cropの中心位置を確認する。

## 追記：表示バージョン運用を変更（M7 v37）
- ユーザーから「v36が続くと最新版が反映されたか判別しにくい」と指摘。
- seam補正までの最新カメラ版を **M7 v37** として表示するよう変更。画面左下のbuild badge、asset query、PWA cache、関連テストを更新。
- 今後は、iPhone実機で確認してもらう内容が変わるレベルのカメラ修正を公開した場合、原則として表示バージョンも1つ進める。同じ表示バージョンのまま複数回ユーザーへ再テスト依頼しない。
- 内部ファイル名・CSS class（例：m7-camera-v36.js / m7v36-*）は互換性と変更範囲縮小のため直ちには改名しない。ユーザーが確認するbuild badgeを正本とする。
- PR #8をmainへマージ。merge commit: `4c9a8ac01110258438558ec4d3e1ba553f82ed25`。

## 追記：v37実機0/14→M7 v38へ
- ユーザーの比較スクリーンショットで、上側の旧M7 v36が1/14、下側の最新M7 v37が0/14。左下build badgeがv37になっているため、最新版反映自体は確認できた。
- v37では複数cropに隣接牌・木目が入り、シーム補正が実牌模様の縦エッジを境界と誤認している回帰を確認。v37での追加学習は打ち切り。
- v38では認識経路を安定していた牌列14等分へ戻したうえで、牌面正規化後の生輝度比較を廃止。背景輝度に対する暗さと色差から12×18のインク特徴を作り、照明差に強い比較へ変更。
- m7-recognition-core.js は216次元（12×18）特徴でも±1セルのshift-tolerant比較を行うよう拡張。新特徴は旧テンプレートと混ぜず MahjongScoreApp_tile_templates_m7v38ink1 を使用。
- 同一記号の明暗差画像が別記号より近くなるブラウザ回帰テストを追加し、固定枠撮影・14分割・初回学習保存・通常対局/採点を含むCIは全成功。
- 表示バージョンを M7 v38 に更新。PR #9をmainへマージ。merge commit: b82016ed9178422c1b9604ab44dec0c04bc2e378。
- 次回実機確認はv38表示を確認し、初回14枚学習→同じ14枚をもう一度撮影して2回目の自動候補数を測る。v37は再テストしない。

## 追記：v38シャッフル8候補中4誤り→M7 v39
- 同じ14枚を並び替えた実機テストで、牌列特定に一度失敗した後、再撮影では8/14枚を自動候補化。ただしユーザー確認は4枚正解・4枚誤り。候補precision 50%のため「候補数が増えた=実用化」とは扱わない。
- v38の自動採用条件（distance<=0.30、2位との差>=0.012）が学習初期として緩く、曖昧な最近傍も確定候補にしていたため、v39はprecision優先へ変更。distance<=0.22、2位との差>=0.028、1位/2位比<=0.82を満たす高信頼候補のみ自動入力する。唯一候補しかない場合もより厳しいdistance<=0.16とする。
- 同時に、未確定/修正対象をタップすると牌選択画面の上部に上位3候補をショートカット表示する機能を追加。自動入力を保守的にしても修正負担を抑える。
- 特徴量形式・localStorageキーは変更せず、v38までに学習したテンプレートをv39で継続使用できる。
- 表示版数をM7 v39へ更新。Chromiumで高信頼/曖昧/遠距離候補の受理・棄却、上位3候補UI、14枚学習保存、通常対局/採点フローを確認しCI成功。

## 追記：v39実機 precision 100%・Top3 recall 11/13
- M7 v39をiPhone実機で同じ14枚のシャッフル手牌に対して確認。
- 高信頼自動候補は1/14枚で、その1枚は正解（auto precision 1/1 = 100%）。v38の8候補中4正解（50%）から、precision優先の狙い通り誤自動入力を抑えられた。
- 残り13枚を「?」から開き、上位3候補に正解牌が含まれていたのは11/13枚（Top3 hit rate 約84.6%）。2枚はTop3外。
- これは「ランキング自体は多くの牌で正解近傍まで来ているが、自動確定のconfidence条件と学習テンプレート密度がまだ不足」という状態。現時点で閾値を再度緩めるのではなく、まずこのシャッフル手牌を14枚正しく確定して2つ目の撮影条件を学習させ、別順序でもう1回だけ測る。
- 次の評価指標は (1)高信頼候補数、(2)そのprecision、(3)未確定牌のTop3 hit rate。候補数だけで良否を判断しない。

## 追記：v39追加学習後に悪化→M7 v40
- 追加学習後の別順序実機テストで、高信頼候補6/14のうち正解2・誤り4（precision約33%）。未確定12枚のTop3に正解が含まれたのは4/12（約33%）。前回の1/1正解・Top3 11/13から悪化したため、「学習枚数を増やせば現在のnearest-template方式が自然に改善する」という仮説を棄却。
- 原因調査で、各ラベルの複数テンプレートの**最小距離1件だけ**を順位に使っていることを確認。テンプレートが増えるほど誤ラベルにも偶然近い1件が出やすいため、v40では各ラベルの近い最大3テンプレートの平均を使うrobust rankingへ変更。1サンプルのみのラベルにはpenaltyを加え、自動確定はrobust scoreとbest raw distanceを併用する。
- UI実機指摘：近い候補を選ぶと連続入力が次牌へ進むが、Top3候補が前牌のまま残った。v40ではpickerの現在indexを追跡し、近い候補/通常一覧どちらから選んでも自動advance後に次牌のTop3へ即更新する。
- 特徴量形式とlocalStorageキーは変更していないため、既存v38/v39学習データをv40でも利用する。表示版数をM7 v40へ更新。
- unit testで最小距離の偶然一致にrobust rankingが耐えること、Chromiumで次牌へ候補が更新されることを追加し、全CI成功。

## 追記：v40実機1/1正解・Top3 4/13・通常修正UI不具合→M7 v41
- M7 v40実機結果：高信頼候補1/14でその1枚は正解（precision 100%）。残り13枚のTop3に正解が含まれたのは4/13。robust rankingで誤自動入力は抑えられているが、ランキングrecall自体はまだ低い。
- UI不具合：近い候補から選ぶ経路は次牌Top3へ更新されるが、通常34牌一覧から選ぶ経路は次牌へ自動advanceしても前牌のTop3が残った。
- v41では認識アルゴリズムをあえて変更せず、UI同期だけを独立修正。picker titleの変更監視＋複数delay再同期により、通常一覧/近い候補/その他picker内操作のどの経路でもactive indexに合わせてTop3を更新する。
- 変更を分離した理由：v40の認識結果とUI修正を混ぜると、次回実機結果で改善要因を切り分けにくいため。v41でUI修正だけ確認後、認識本体はfeature/descriptor側の再設計を検討する。
- 表示版数はM7 v41。Chromium回帰テストを含むCI成功。
