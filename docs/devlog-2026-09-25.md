# MahjongScoreApp 開発記録 2026-09-25

## 作業時間
- ユーザー申告：1時間00分（9月24日夜〜25日未明の会話・作戦整理）。
- 更新後の累計：26時間00分。正本は `docs/development-time.md`。

## 本日の作業・確認
- 旧チャットの引継ぎ情報を確認。GitHub main の引継ぎ時点の最新コミットは `9bc196d3c3cc54bf7b5686ad130a155e758ab62d`、アプリ表示は M7 v35。
- ユーザーが iPhone で実牌14枚を机の上に密着させて横一列に並べて撮影。カメラ画面は候補 `0/14` のまま。確認ボタンで確認画面へ進めたが、牌列が抽出されず撮影画像全体＋14個の未確定欄が表示された。自動牌種認識も実行可能な切り出し画像がなく、一枚も認識されていない。
- この結果は、v35 の「列が見つからなければ全画像を表示する」フォールバック自体は動いたことを示すが、牌列検出・14枚切り出し・牌種認識の完成は示さない。
- 今回は症状と開発方針のみ整理。実牌で検出が0になるコード上の直接原因は未確定。新たなコード実装・公開・自動テスト・iPhone合格判定は行っていない。

## 今回決定した仕様・開発方針
- 認識開発方針は **C：まず手動の補助操作も使って実用化し、その後に完全自動認識へ段階的に進める**。
- 撮影方法は **A：アガった手牌を机に寝かせ、14枚を横一列に並べて上からiPhoneで撮影する**。立てた牌への対応を今回の必須条件に追加しない。
- 目標は、牌列を自動発見→13〜14枚の切り出し・枚数認識→牌種自動判定→誤りだけ修正→点数計算。手動切り出しだけでM7全体を完了扱いにしない。
- 次版（仮称 M7 v36）案：既存自動検出を残し、失敗時は撮影画像に牌列全体の範囲指定・位置/サイズ調整を設け、その範囲だけを14分割して各牌の実写プレビューを表示する。正しい牌名を指定・確定できるようにする。14等分がずれる場合の境界補正は実画像で評価する。保存済みの実牌切り出し画像と正解ラベルを再利用して、認識方式の精度を検証する構想。
- 次版の具体的な修正内容と試験は、最新main・管理ファイル・既存カメラ処理を確認して確定する。バージョン番号も実装時に最新状態から決める。
- 競合アプリの実機UIを参考にし、M7の方針を追加更新することをユーザーが承認。**リアルタイムで14個の物体を完全検出してから進むことを必須にせず、固定の手牌撮影枠＋シャッター方式を中心にする。** 撮影後に枠内を解析し、牌列補正→14枚分離→牌種認識→誤認識だけ高速修正→和了牌指定→採点、という流れを目標とする。
- 競合アプリから参考にするのは機能設計・UXの考え方であり、コードや画像素材を流用しない。ドラ表示牌の別枠撮影、追加役の手動指定、符・役・基本点の説明表示は将来候補。現在のM7ではまず手牌14枚の読み取りを優先する。
- 次回M7では、実画像による0/14原因調査、高解像度撮影画像の保持、固定撮影枠、シャッター後解析、誤認識修正UIを優先的に検討する。

## 未完了・注意
- M7 v35 の実牌検出は不合格。候補0/14、実牌列の位置推定失敗、個別画像未取得。根本原因は未確定。
- 役・符・点数計算の次段階や副露・カン入力に勝手に進まない。
- iPhone実機での最終合格報告なし。作戦整理終了につき本日の開発はここまで。

## 次回の開始地点
1. GitHub main と `AGENTS.md`、`docs/user-development-rules.md`、`docs/pre-change-checklist.md`、`docs/known-mistakes-and-prevention.md`、M7 カメラ本体・認識コア・関連テストを確認。
2. 0/14 と牌列推定失敗の根本原因を、今回の実牌画像とコードで調査。「確定」「仮説」「実機ログが必要」を区別する。
3. 同じカメラ段階で **検出失敗への修正＋手動範囲指定・14枚切り出し・個別プレビュー** をまとめて実装する（仮称 M7 v36）。
4. 可能な自動テスト・GitHub Pages 公開状況を確認してから、一度のまとまった iPhone 実機チェックを提示。ユーザーのOKまで次の段階へ進まない。

## M7 v36 実装（iPhone最終確認前）
- v35の `0/14` についてコードと実機スクリーンショットを再調査。ライブ候補検出が480×190全フレームに対して高さ34%以上を要求しており、現在の実機構図の牌サイズと不整合だったことを確認。固定RGB明るさ条件も実画像に厳しい可能性が高かった。
- ユーザー承認済みの方針に従い、**固定手牌枠＋シャッター＋撮影後解析**へ変更。ライブで13〜14個の候補が揃うことを撮影条件から外した。
- CSS上の白枠と `object-fit: cover` されたvideoの表示座標を元動画座標へ変換し、撮影時は白枠部分を高解像度で切り出す。
- 撮影後は輝度と色差の比を使う適応的な牌列推定を行い、成功時のみ14分割した実写プレビューと端末内学習候補を表示する。失敗時は白枠内の撮影画像＋14個の手動入力へフォールバックする。
- 撮影ボタンを画面下中央へ配置し、キャンセルとは分離。カメラが非表示になった場合はキャンセル経路を通してMediaStream停止を試みる。
- 暗めの木目背景と低輝度の牌列を使うブラウザ回帰テストへ更新。公開バージョンは M7 v36 として準備。
- まだiPhone実牌テストは未実施。**v36を合格扱いにしない。**
